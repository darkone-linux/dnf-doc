---
title: Les alertes Prometheus
sidebar:
  label: Alertes Prometheus
  order: 5
lang: fr
---

{/* t:main */}
{/* t:h 02478102e8b0de9d619cdaa5ec51891c04b18851632de1e0309e138db2c4b243 */}

{/* t:p 9dec07a2d83662f2ee6a730c7e6db499d839906542e2b5eb8b9648c54901a899 */}
Carte de maintenance du code d'alerte : où se trouve quoi, et comment l'étendre
sans rien casser. Le fonctionnement côté administrateur est décrit dans
[Monitoring & Alertes](/fr/doc/admin-guide/understand/monitoring-alerts/).

{/* t:p 9bcae6b491f58013270b8a61730e99b3ef9c1a5888461f94e10fb09d5a505cea */}
## Logique pure vs câblage

Le code est scindé en deux, pour garder la génération des règles **testable** :

- `dnf/lib/alerts.nix` : fonctions **pures** qui produisent les règles Prometheus
  à partir de la topologie. Testées dans `dnf/tests/unit/lib/alerts_test.nix`.
- `dnf/modules/service/prometheus.nix` : **câblage** impur (Alertmanager, routage
  par sévérité, sops, bot Matrix, vhost, sondes blackbox).

Toute logique non triviale va dans `alerts.nix` ; le module se contente de la
brancher.

{/* t:p 455e583701bd20d8de414032ee9a6897ff834c8ad2d3b7d9fe001ae9ed17d3df */}
## Les helpers (`alerts.nix`)

Exposés via `dnfLib` (voir `dnf/lib/default.nix`) :

| Helper | Rôle |
|---|---|
| `serviceUnits` | Service DNF → unité systemd (ex. `idm` → `kanidm.service`) |
| `nodeClass` | Classe d'un nœud (critique / non-critique / désactivé) : features `alert-*` puis profil |
| `nodeAlertEligible` | Sélection : un nœud n'est surveillé que s'il est nœud réseau/serveur, porte `alert-non-critical`, ou héberge un service mappé |
| `severityForClass` | Classe → sévérité (`critical` ou `warning`) |
| `hostExpectedUnits` | Unités attendues d'un hôte (d'après ses services activés) |
| `mkNodeRuleGroups` | `NodeDown`, `ServiceDown`, `SystemdUnitFailed` (+ label `reach` local/wan) |
| `mkResourceRuleGroups` | Disque, RAM, charge, inodes, OOM, FS read-only, prédiction disque, horloge, conntrack |
| `mkNetworkRuleGroups` | Sondes blackbox (passerelle, tailnet, DNS, `ZoneInternetDown`) |
| `mkHttpRuleGroups` | Sondes HTTP : `ServiceEndpointDown` + expiration de certificat TLS |
| `mkResticRuleGroups` | Fraîcheur des sauvegardes (`ResticBackupStale`/`Critical`) |
| `mkSmartctlRuleGroups` | Santé disque SMART (`DiskSmartFailing`, `DiskTemperatureHigh`) |
| `mkPostfixRuleGroups` | Relais Postfix (`PostfixRelayUnhealthy`, `PostfixDeferredQueueHigh`) |
| `mkSynapseRuleGroups` | Synapse (`SynapseRestarting`, `SynapseHighErrorRate`) |
| `mkMaintenanceRuleGroups` | Drapeau de maintenance (silence pendant rebuild) |
| `mkTailscaleRuleGroups` | Auto-réparation tailnet (`TailscaleUnhealthy`, `TailscaleFlapping`) |
| `mergeRuleGroups` | Fusionne des fragments en **un seul** document |
| `mkAlertRuleGroups` | Raccourci : nœuds + ressources + restic + SMART + tailscale |
| `mkSilenceRoutes` | Routes Alertmanager vers le receiver `null` (alertes connues, acceptées) |

{/* t:p 3a2c869b4919f9263d282820c9cbb697290831064b24501d391499b2482005f8 */}
### Le label `host`, ou pourquoi les alertes se nomment

Chaque cible de scrape porte un label `host` valant le nom d'hôte, posé par
`mkHostTargets` dans `prometheus.nix` (un `static_config` par hôte plutôt qu'une
liste unique de cibles). Deux effets :

- le bot Matrix affiche `DiskSpaceLow at srv-backup` au lieu de l'instance brute
  `<ip>:<port>` ;
- une route de silence peut viser une seule machine.

Le poser au scrape le propage gratuitement à toutes les alertes issues de ces
jobs, y compris les règles ressources génériques, qui ignorent tout de la
topologie à l'évaluation.

:::caution[Les agrégations effacent les labels]
Une clause `by (...)` supprime tout label non groupé. `ResticBackupStale`
(`max by (instance, job, host)`) et `SynapseHighErrorRate`
(`sum by (instance, host)`) citent donc `host` explicitement. L'oublier dans une
nouvelle règle agrégée fait retomber la notification sur l'instance brute.
:::

:::note[Le bot n'affiche pas `summary`]
Le bot Matrix rend le titre puis l'annotation `description`, et ne se rabat sur
`summary` que si `description` est absente&nbsp;: toutes les règles DNF en
définissent une, donc `summary` ne lui parvient jamais. Une information à faire
figurer dans la notification doit aller dans `description`.
:::

{/* t:p 7c13d3bbffc588d69c9063078871bec5dde8fca22801d89ebdad825c59ee2d66 */}
### Sélection des nœuds surveillés

La supervision est *opt-out* pour l'infrastructure, *opt-in* pour le reste.
`nodeAlertEligible` retient un nœud uniquement s'il est de classe `critical`
(profil `gateway`/`hcs`/`server` ou feature `alert-critical`), s'il porte
explicitement `alert-non-critical`, ou s'il héberge au moins un service mappé
dans `serviceUnits`. Un laptop/desktop nu ne génère donc aucune alerte node.

{/* t:p 7118836dd86dace4a9bc592f0d10cccaee095656e2ba723fc757ac17b196ed87 */}
### Panne internet vs hôte distant down

Le Prometheus d'une zone joint les hôtes des autres zones (ex. le HCS public)
uniquement via le WAN. Quand internet tombe, ces cibles deviennent injoignables
et déclencheraient un faux « *host* down ». Pour l'éviter :

- chaque règle node porte un label `reach` (`local` même zone, `wan` cross-zone) ;
- une sonde blackbox ICMP vers des IP externes alimente `ZoneInternetDown`
  (déclenchée seulement si **toutes** les cibles externes échouent) ;
- une règle d'inhibition Alertmanager (`equal: [zone]`) fait taire les alertes
  `reach="wan"` de la zone tant que `ZoneInternetDown` est active — la vraie
  cause est notifiée, pas les symptômes.

{/* t:p c5d86961939c4492ce81c4a9d92772bf2385dfb6d920af9820758e2928e60fc4 */}
## Le piège : un seul document de règles

:::caution[À ne jamais oublier]
NixOS **concatène** toutes les entrées de `services.prometheus.rules` (une liste
de chaînes) dans **un seul fichier**. Plusieurs documents `groups:` sans
séparateur `---` → **seul le premier est lu**. Empiler une deuxième entrée casse
**silencieusement** les groupes suivants.
:::

On fusionne donc **tout** via `mergeRuleGroups`, puis on n'émet **qu'une** entrée :

```nix
# dnf/modules/service/prometheus.nix
services.prometheus.rules = [
  (builtins.toJSON (dnfLib.mergeRuleGroups (
    [ (dnfLib.mkAlertRuleGroups { inherit nodes; /* … */ }) ]
    ++ lib.optional alerting.silenceOnRebuild (dnfLib.mkMaintenanceRuleGroups { /* … */ })
    ++ lib.optional alerting.network.enable (dnfLib.mkNetworkRuleGroups { /* … */ })
  )))
];
```

{/* t:p 97c1f486cf7857b104bd4a84f6cbae530541aa913a4c7e490fcb69b16928eb5f */}
## Où ajouter quoi

| Je veux… | Je touche… |
|---|---|
| surveiller un nouveau service | `serviceUnits` dans `alerts.nix` |
| une nouvelle famille de règles | un `mkXRuleGroups` + l'ajouter au `mergeRuleGroups` du module |
| câbler un exporter de métriques | exporter dans son module (gated `monitoring-node`, bind `preferredIp`) + job de scrape + `mkXRuleGroups` gated dans `prometheus.nix` |
| changer un seuil par défaut | `defaultThresholds` dans `alerts.nix` |
| ne plus alerter sur un FS monté `ro` volontairement | `readOnlyByDesignFstypes` (seuils) dans `alerts.nix` |
| surveiller (ou non) l'espace d'un montage réseau | `remoteFstypes` (seuils) dans `alerts.nix` |
| changer la classe d'un nœud | feature `alert-*[:zone]` (aucun code) |
| forcer/exclure un nœud de la surveillance | feature `alert-non-critical` / `alert-disabled` |
| surveiller une URL + son certificat TLS | `alerting.network.httpProbeUrls` dans la config |
| ajuster la détection de panne internet | `alerting.network.internetProbeTargets` |
| taire une alerte connue et acceptée | `alerting.silences` dans un module consumer (aucun code) |
| taire une unité systemd cassée sur tout le parc | `ignoredUnits` dans `dnf/config/alerts.nix` |
| une nouvelle destination | le bloc `alertmanager` de `prometheus.nix` (receiver + route) |

:::note[Exclure plutôt que taire]
Une condition permanente et voulue ne se met pas sous silence&nbsp;:
`alerting.silences` coupe la notification mais laisse l'alerte se déclencher,
donc rougir les UI, pour toujours. Elle doit sortir du **sélecteur** de la règle.
Deux listes de seuils le font, dans `mkResourceRuleGroups`&nbsp;:

- `remoteFstypes` — retiré de **toutes** les règles FS. Un montage client
  remonte l'espace du **serveur**&nbsp;: le surveiller transforme un export
  plein en une même alerte sur chaque machine qui le monte, alors que le serveur
  alerte déjà tout seul, avec le vrai point de montage.
- `readOnlyByDesignFstypes` — retiré de `FilesystemReadOnly` seul (médias
  optiques). La règle ne surveille donc que les montages censés être accessibles
  en écriture.
:::

:::tip[Convention DNF]
Un helper de `lib/` = un test. Toute fonction ajoutée à `alerts.nix` se double
d'un cas dans `dnf/tests/unit/lib/alerts_test.nix` (`nix-unit`).
:::

:::caution[Ordre des routes Alertmanager]
`continue` vaut `false` par défaut&nbsp;: la première route enfant qui matche
gagne. Les routes de `mkSilenceRoutes` doivent donc être insérées **avant** les
routes par sévérité, sinon elles ne sont jamais atteintes et l'alerte notifie
quand même. Le receiver `null` est partagé avec la route de maintenance, d'où sa
garde `silenceOnRebuild || silences != []`&nbsp;: référencer un receiver absent
empêche Alertmanager de démarrer.
:::

:::caution[Métrique textfile : permissions]
Un fichier `.prom` du collecteur textfile doit être lisible par `node-exporter`
(utilisateur non-root). `mktemp` crée en `0600` : poser un `chmod 0644` **avant**
le `mv` atomique, sinon le fichier est ignoré (`node_textfile_scrape_error 1`) et
la métrique n'est jamais exposée, donc l'alerte reste aveugle.
:::
