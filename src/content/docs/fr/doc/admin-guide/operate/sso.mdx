---
title: SSO et identités (Kanidm)
sidebar:
  label: SSO (Kanidm)
  order: 2
lang: fr
---

{/* t:main */}
{/* t:h 62cb531d6b0df6f948c43272fe531bd1056b2fcee8e6f4e1b4f8b24409eaf5eb */}

{/* t:p 1947e9b785d3587ff9594517ac977ebba27628e04749283826a37b1e3ea6ed1e */}
[Kanidm](https://kanidm.com/) est le fournisseur d'identité du réseau : une
**identité unique** ouvre tous les services (SSO). Le service `idm` tourne sur
le HCS (et, en option, en réplica dans chaque zone).

:::tip[Comprendre le fonctionnement]
Le détail des mécanismes (OIDC natif, reverse proxy authentifiant, flux de
connexion) est expliqué dans
[Authentification et identités](/fr/doc/admin-guide/understand/auth-idm/).
:::

{/* t:p c3f1ee94735a2710129012e89644385de01f1bbe56d240cb932d0d5cd784889f */}
## Identités et groupes

Comptes et groupes sont provisionnés dans Kanidm depuis `etc/config.yaml` :

```yaml
# etc/config.yaml
users:
  alice:
    profile: "nix-admin"
    groups: ["idm-admins", "idm-devs", "global"]
```

- Les **groupes** pilotent les accès aux services.
- Deux groupes spéciaux : `idm-admins` (administration), `idm-devs` (développement).

{/* t:p ac1fc81e7b77d2baf389c63217472b84acd3acbd151f093ba937b3d251d6b666 */}
## Connecter un service (OIDC)

Les services compatibles OIDC sont reliés au SSO **automatiquement** : Kanidm
provisionne un client OAuth2 par service, et son secret est géré par
[sops](/fr/doc/admin-guide/operate/secrets/). Rien à configurer à la main.

:::tip[Exemples]
Outline, Mealie, Vaultwarden, Grafana, Immich, Forgejo, Jellyfin, Open WebUI…
:::

{/* t:p 64a6a287516b8f5184000146d34d675d99736d3800ab68bd21885d1f4658ec2e */}
## Protéger un service sans OIDC

Un service sans authentification propre (page d'accueil, site statique) se
protège en plaçant une **mire de connexion Kanidm devant lui** (oauth2-proxy +
Caddy). Pour la page d'accueil, deux réglages sur l'hôte passerelle
(`usr/machines/<passerelle>/`) :

```nix
darkone.service.homepage.protect = true;             # exige une session Kanidm
darkone.service.homepage.protectExternalOnly = true; # interne libre, externe authentifié
```

| Option | Défaut | Effet |
|---|---|---|
| `protect` | `true` | Page d'accueil réservée aux utilisateurs Kanidm (groupe `users`) |
| `protectExternalOnly` | `false` | LAN/VPN sans connexion, login requis depuis l'extérieur |

:::note[Pré-requis]
Le service `idm` doit tourner (HCS) et le service `homepage` être présent dans la
zone : il sert d'**ancre** au flux de connexion. Le fonctionnement est détaillé
dans [Authentification et identités](/fr/doc/admin-guide/understand/auth-idm/).
:::

{/* t:p b1d628c6ebd9bd33b085868abcce9b72c5f9e08792ac1cb8f8bb340e4524e1e8 */}
## Réplication multi-zone

:::danger[Pas encore fonctionnel]
La réplication multi-zone est **en cours de développement** et n'est pas
opérationnelle. Pour l'instant, déclarer `idm` **sur le HCS uniquement**.
:::

Le mode est déduit de l'endroit où `idm` est déclaré :

1. `idm` sur le **HCS seul** → instance unique, pas de réplication.
2. `idm` sur une **passerelle sans HCS** → instance autonome dans la zone.
3. `idm` sur le **HCS et des passerelles** → réplication : le HCS fournit, chaque passerelle est un réplica en lecture seule.

```d2
style.fill: transparent
direction: down

hcs: HCS, fournisseur\nWriteReplica { shape: cylinder }
gwa: Zone A, réplica\nlecture seule { shape: cylinder }
gwb: Zone B, réplica\nlecture seule { shape: cylinder }

hcs -> gwa
hcs -> gwb
```

:::caution[Amorçage en deux temps (scénario 3)]
1. `just apply` : chaque nœud génère son certificat de réplication.
2. `just idm-sync-certs` puis `just apply` : les passerelles passent en lecture seule (pull depuis le HCS).
:::

{/* t:p 694e5fdf9be2e87257305796f83a3e911d7fccb0826cdae46c46e13aa9866a7f */}
## Administration

L'administration se fait en ligne de commande sur le HCS :

```sh
just enter hcs
kanidm person credential create-reset-token <login> --name idm_admin
```

Voir [Créer un compte utilisateur](/fr/doc/admin-guide/install/hcs/#créer-un-compte-utilisateur)
et [Réinitialiser un mot de passe](/fr/doc/how-to/admin/reset-user-password/).
