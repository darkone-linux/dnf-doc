---
title: Chiffrement des disques (LUKS)
sidebar:
  label: LUKS
  order: 2.7
lang: fr
---

{/* t:main */}
{/* t:h 254e857338e9595c03a7667fac194bc95e30f99e6371ec6670586678427032f6 */}

{/* t:p af783705169f6a4bc72b07e7fb250335108742038a247c22e6eb804ab281f74e */}
Un hôte chiffré (volume LUKS2 déclaré dans sa configuration disko) protège ses
données en cas de vol ou de mise au rebut, mais réclame une passphrase à chaque
démarrage. DNF industrialise ce point de friction avec deux garanties :

- **plusieurs moyens de déverrouillage coexistent** en permanence, la perte
  d'un moyen (passphrase oubliée, YubiKey égarée) n'enferme jamais dehors ;
- **le prompt du boot se répond à distance**, depuis le poste
  d'administration, sans écran ni clavier branchés sur l'hôte, et cela dès le
  **tout premier démarrage** qui suit l'installation.

{/* t:p edd729de32f9e8fb828be810f1268c4594886c1436b1d4695863408a30b84661 */}
## Les moyens de déverrouillage

Chaque moyen occupe son propre keyslot dans l'en-tête LUKS2. Tous sont valables
en même temps, sur chaque volume chiffré de l'hôte.

| Moyen | Où il vit | Rôle |
|---|---|---|
| **Passphrase partagée** | sops → `luks-passphrase` | Une seule pour tout le parc, autorise la gestion des keyslots. C'est aussi elle qui **formate** le disque |
| **Passphrase par hôte** | sops → `luks/<hôte>/passphrase` | À confier à l'utilisateur de la machine sans exposer le parc |
| **YubiKeys FIDO2** | Dérivé de la clé physique | Toucher la clé au boot, rien à taper |

:::note[Pourquoi deux passphrases et pas une]
Elles ne s'adressent pas aux mêmes personnes&nbsp;: la partagée est la clé de
l'administrateur du parc, celle par hôte se confie à l'utilisateur d'une seule
machine. Et parce qu'elles sont deux, la rotation de l'une reste **autorisée
par l'autre**&nbsp;: le service de synchronisation ne se retrouve jamais sans
moyen d'agir sur les keyslots, il ajoute le nouveau slot avant de tuer l'ancien.
:::

:::note[Il n'y a plus de passphrase d'installation]
`just install` formate le volume avec la passphrase partagée
(`nixos-anywhere --disk-encryption-keys`)&nbsp;: le keyslot de formatage **est**
le keyslot partagé. Rien à retenir de l'installation, rien à retaper ensuite.
Un hôte installé avant cette bascule est détecté par `just luks <hôte>`, qui
demande alors sa passphrase d'installation une dernière fois pour amorcer le
slot partagé.
:::

:::note[Rien à déclarer dans la configuration Nix]
Le module `darkone.system.luks` est actif par défaut mais **totalement inerte**
tant que l'hôte n'apparaît pas dans le manifeste `usr/secrets/luks.json` **et**
ne déclare pas de volume LUKS dans son disko. Les hôtes chiffrés existants ne
bougent pas tant que `just luks <hôte>` n'a pas été lancé.
:::

{/* t:p 5469bdbe1ad7616c7b3e8e25a6dffb29a16ba52dfb6eb7c27e19841bfcc9d8d4 */}
## Provisionner un hôte

```sh
just luks <hôte>            # provisionne (ou converge) le chiffrement
just luks <hôte> pre        # moitié hors ligne, sans toucher à la machine
just luks <hôte> passwd     # fait tourner la passphrase par hôte
just luks <hôte> show       # audit : table des keyslots + conformité
just luks <hôte> '' <ip>    # cible une IP explicite (hôte pas encore résolu)
```

La recette est **idempotente**&nbsp;: chaque exécution converge et ne demande
que ce qui manque. Elle est appelée automatiquement à deux moments, et il est
rare d'avoir à la lancer soi-même&nbsp;:

| Appelée par | Action | Ce qu'elle fait |
|---|---|---|
| `just install <hôte>` | `pre` | Avant le build, donc avant l'installation |
| `just configure <hôte>` | (défaut) | Après le premier démarrage |

Sans volume LUKS dans le disko, c'est un no-op journalisé.

La phase **`pre`** ne touche jamais la machine. Elle prépare ce qui doit
exister *avant* que le système ne soit construit&nbsp;:

1. la **passphrase partagée** dans sops si absente (demandée une seule fois
   pour tout le parc) ;
2. la **passphrase par hôte** dans sops si absente (saisie vide = valeur
   aléatoire forte, l'hôte reste déverrouillable par la partagée et les
   YubiKeys) ;
3. la **clé d'hôte SSH de l'initrd**, générée localement et rangée dans sops
   (`luks/<hôte>/initrd-key`, encodée en base64) ;
4. l'inscription de l'hôte dans le **manifeste public** `usr/secrets/luks.json`
   (versionné en clair, il ne contient aucun secret)&nbsp;: c'est cette entrée
   qui active le module.

La phase par défaut ajoute ce qui demande la machine&nbsp;:

5. repose la **clé d'initrd** depuis sops si elle manque sur la cible (elle
   n'en régénère jamais une nouvelle, cela invaliderait celle qu'a posée
   l'installation) ;
6. sur une **passerelle**, relève l'**IP WAN** courante dans le manifeste
   (repli de `just enter` et `just unlock`, voir plus bas), à relancer si le
   fournisseur d'accès renumérote ;
7. si **aucune passphrase gérée** ne déverrouille le volume (hôte installé
   avant la bascule vers le formatage partagé), demande sa passphrase
   d'installation une dernière fois et enrôle la partagée avec.

Puis committer et déployer&nbsp;:

```sh
git add usr/secrets/luks.json
just commit "system(luks): <hôte>"
just apply <hôte>
```

Au déploiement, deux services idempotents convergent l'en-tête LUKS à chaque
apply : `luks-passphrase-sync` (passphrases partagée et par hôte) et
`yubikey-luks-enroll` (une entrée FIDO2 par YubiKey enrôlée sur le parc, voir
[Authentification forte](/fr/doc/admin-guide/operate/yubikey/)).

:::caution[La clé d'initrd doit précéder le chargeur d'amorçage]
Les secrets d'initrd sont embarqués **à l'installation du chargeur
d'amorçage** : une clé créée après coup n'est tout simplement pas dans
l'initrd qui démarre. Une installation neuve est couverte, `just install`
dépose la clé sous `/mnt` avant `nixos-install`. Sur une machine déjà en
service, c'est `just luks <hôte>` qui la pose par SSH&nbsp;: ne jamais ajouter
un hôte à `luks.json` à la main, et ne jamais déployer avant que la clé ne soit
sur la cible. En cas de doute, `just luks <hôte> show` le vérifie.
:::

{/* t:p 0d4399f24cf21c90398100bb7d0dfd5dedfac86467e4dc46cd2c5bf96ee18b6a */}
## Rotation des passphrases

- **Par hôte** : `just luks <hôte> passwd`, puis commit et apply. L'ancien
  keyslot est supprimé et remplacé au déploiement suivant.
- **Partagée** : modifier `luks-passphrase` via `just sops`, puis apply sur
  tout le parc — chaque hôte chiffré fait tourner son keyslot au passage.

Le service de synchronisation détecte les changements grâce à un registre
local (`/var/lib/luks-passphrase/ledger`)&nbsp;: une valeur sops modifiée
enrôle la nouvelle passphrase **puis** tue l'ancien slot. Les slots absents du
registre (FIDO2, enrôlements manuels) ne sont jamais touchés.

{/* t:p 279014f484ff79e95e7ed795c00ede1f46858003c4f3ca8ba9ae3cd0cf7705b7 */}
## Déverrouiller au démarrage

Tout hôte chiffré suit la même séquence, sans rien à régler :

1. **YubiKey** — une clé enrôlée et branchée fait clignoter sa diode : la
   toucher ouvre le volume, rien à taper. Aucune clé branchée, l'étape dure
   ~6 s ; une clé que personne ne touche, ~30 s (limite du firmware).
2. **Passphrase** — une invite est ensuite posée, **sans limite de temps**.
   Elle accepte la passphrase par hôte, la partagée ou celle d'installation.

L'invite de l'étape 2 est **une seule et même demande**, servie en parallèle
sur la console physique (`/dev/tty1`) et par ssh sur le port 2222. Rien à
choisir, aucune fenêtre à rater : le premier qui répond ouvre le volume.

:::note[Comment la clé est essayée]
`fido2-device=` n'est pas écrit dans l'entrée `crypttab` mais dans une **unité
initrd séparée**, `yubikey-luks-unlock-<volume>`, ordonnée avant
`systemd-cryptsetup@<volume>` et seulement reliée à elle par un `Wants=` :
elle a le droit d'échouer, et son échec ne change rien au démarrage.

Cette séparation évite deux défauts qui se contredisent :

- **l'option dans le `crypttab` tue le démarrage** quand une clé est branchée
  sans que personne la touche : `systemd-cryptsetup` sort en erreur au lieu de
  proposer la passphrase, aucun prompt n'est posé — ni console, ni port 2222 —
  et l'initrd bascule en `emergency`. Une coupure de courant sur un hôte sans
  personne devant suffisait à l'immobiliser ;
- **sans l'option, le token n'est essayé que s'il est déjà énuméré**. Or
  `hidraw0` apparaît environ une seconde après le lancement de
  `systemd-cryptsetup` : jamais à temps. La clé cesse alors de déverrouiller
  quoi que ce soit, en silence, et sans rien dans le journal.

Ce que l'option apporte réellement, c'est l'**attente** du périphérique
(`token-timeout=`, réglé à 5 s).
:::

À distance, l'initrd démarre un **sshd dédié sur le port 2222**, avec sa
propre clé d'hôte persistante, accessible uniquement avec la clé de
déploiement `nix`. Deux façons d'y répondre&nbsp;:

```sh
just unlock <hôte>     # sans humain : la passphrase est lue dans sops
just enter <hôte>      # interactif : le prompt est présenté au terminal
```

`just unlock` attend que le port 2222 apparaisse (un point par tentative), lit
la passphrase par hôte dans sops (repli sur la partagée) et la pousse dans
l'initrd. C'est ce que `just full-install` appelle tout seul après le premier
redémarrage. Sur un hôte non chiffré, c'est un no-op journalisé.

`just enter` fait la même chose en mode interactif&nbsp;: quand le port 22 ne
répond pas, il sonde le 2222 sur l'IP explicite (si fournie), le nom d'hôte,
puis l'**IP WAN enregistrée dans le manifeste**, et présente le prompt LUKS via
`systemd-tty-ask-password-agent`. Dans les deux cas, la passphrase acceptée, le
démarrage continue et le port 22 revient.

:::note[Pourquoi deux chemins]
`systemd-tty-ask-password-agent` ne parle qu'à un terminal&nbsp;: aucun script
ne peut le piloter. L'initrd embarque donc un petit utilitaire `dnf-unlock` qui
prend la passphrase sur un tube et répond à toutes les demandes en attente.
C'est lui que `just unlock` appelle.
:::

:::note[Pourquoi l'IP WAN ?]
Le VPN d'une zone meurt avec sa passerelle : quand celle-ci attend sa
passphrase, seule sa patte WAN est joignable. Son initrd réplique d'ailleurs
l'adressage de production (IP statique sur le pont `lan0`, DHCP côté WAN),
puisqu'elle est elle-même le serveur DHCP que son initrd attendrait sinon.
Les autres hôtes font un simple DHCP sur leurs interfaces filaires.
:::

:::note[Portables en Wi-Fi]
Pas de réseau en initrd sans câble : un portable chiffré se déverrouille à la
console (passphrase ou YubiKey), le déverrouillage à distance ne s'applique
pas.
:::

## Vérifier un hôte

`just luks <hôte> show` n'écrit rien&nbsp;: il interroge la machine et rend
compte. Il sort en erreur dès qu'un contrôle échoue, donc il s'utilise aussi
dans un script.

```sh
just luks <hôte> show
```

Un premier bloc dresse la **table des keyslots** de chaque volume. Rien n'y est
supposé&nbsp;: chaque passphrase gérée est réellement essayée contre l'en-tête,
donc la colonne « Check » dit ce que le volume accepte aujourd'hui, pas ce
qu'un registre a mémorisé.

```
/dev/disk/by-partlabel/disk-main-luks
  Slot  Content              Source                       Check
  ----  -------------------  ---------------------------  -----------------
  0     Shared passphrase    sops luks-passphrase         opens the volume
  1     Host passphrase      sops luks/poste/passphrase   opens the volume
  2     YubiKey alice/main   systemd-fido2 token          enrolled
  7     Unknown              -                            not managed by DNF
  Slots in use: 4/32
```

Un second bloc audite le **déverrouillage distant**, point par point&nbsp;:

| Contrôle | Ce qu'il attrape |
|---|---|
| Hôte présent dans `luks.json` | Le module resterait inerte |
| Clé d'initrd sur la cible (+ empreinte) | Prochain apply sans déverrouillage distant |
| Sauvegarde sops de même empreinte | Une réinstallation perdrait l'accès distant |
| sshd d'initrd activé dans la configuration | `nix.pub` manquant, ou hôte hors manifeste |
| Chargeur d'amorçage postérieur à la clé | **Le** piège&nbsp;: clé absente de l'initrd qui démarre |
| Système démarré = configuration auditée | Un apply en attente rendrait l'audit trompeur |
| IP WAN relevée (passerelles) | Injoignable en initrd une fois le VPN tombé |

{/* t:p b2e48a2ef1e330c748b75cd9143d12c276cde06e38d46b7033372022b534e739 */}
## Sous le capot

| Emplacement | Contenu | Statut |
|---|---|---|
| `usr/secrets/luks.json` | Manifeste des hôtes provisionnés + IP WAN des passerelles | Public, versionné |
| `secrets.yaml` → `luks-passphrase` | Passphrase partagée du parc | Chiffré sops |
| `secrets.yaml` → `luks/<hôte>/passphrase` | Passphrase par hôte | Chiffré sops |
| `secrets.yaml` → `luks/<hôte>/initrd-key` | Clé d'hôte SSH de l'initrd (base64) | Chiffré sops |
| `/var/lib/luks-initrd/` (cible) | Copie de travail de la clé d'initrd | Persistant, hors dépôt |
| `/var/lib/luks-passphrase/ledger` (cible) | Registre des keyslots gérés (détection des rotations) | Persistant, hors dépôt |

Le module expose deux options : `darkone.system.luks.enable` (défaut `true`)
et `darkone.system.luks.sshPort` (défaut `2222`).

:::danger[Budget de keyslots]
Un en-tête LUKS2 contient au plus **32 keyslots**. Chaque YubiKey enrôlée en
consomme un sur **chaque** hôte chiffré (politique de parc), en plus des
passphrases partagée et par hôte. Le module avertit quand le total projeté
approche de la limite.
:::

{/* t:p dfceb630353cb65ed8c0887148b11be424125fce0a38013882e9f28165469e56 */}
## Voir aussi

- [Authentification forte (YubiKey)](/fr/doc/admin-guide/operate/yubikey/) : l'enrôlement des clés physiques, dont le volet LUKS FIDO2
- [Gestion des secrets](/fr/doc/admin-guide/operate/secrets/) : sops, clés age et rotation
- [Le Justfile](/fr/doc/admin-guide/operate/justfile/) : la référence des recettes `luks`, `enter` et `configure`
