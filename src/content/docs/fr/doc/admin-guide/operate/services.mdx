---
title: Le catalogue de services
sidebar:
  label: Services
  order: 1
lang: fr
---

{/* t:main */}
{/* t:h 486d849ea3fad72033043f922fa22b9d58540522e9687786a106498839d77a20 */}

{/* t:p 3478de3979d02acf830977b4aca60df993f23c57fa8843237fc3c6c232a6516f */}
Les services sont les briques auto-hébergées du réseau. On les active **par
hôte**, dans `etc/config.yaml`. Le profil de l'hôte n'a pas d'importance.

{/* t:p 9825597dfbc253718df3f1d33525c6b0cd6102e2e2d6881b87fffefc17add70b */}
## Activer un service

Sous la clé `services` d'un hôte, chaque entrée active un service. La valeur
(facultative) le personnalise :

```yaml
# etc/config.yaml
    services:
      immich:
        title: "Photos"
        description: "Mes photos & vidéos"
        domain: "photos"          # sous-domaine du service
        global: true              # → https://photos.domain.tld (public, sans zone)
      nextcloud:
        domain: "cloud"           # → cloud.<zone>.domain.tld (non global)
      restic:                     # valeurs par défaut
```

| Champ | Rôle |
|---|---|
| *(clé)* | Le service à activer (ex. `immich`) |
| `title` | Nom affiché sur le portail |
| `description` | Sous-titre sur le portail |
| `domain` | Sous-domaine (défaut : nom du service). FQDN : `<domain>.<zone>.domain.tld` |
| `global` | Expose publiquement via le HCS : `<domain>.domain.tld`, **sans la zone** ([DNS public](/fr/doc/admin-guide/install/hcs/#services-globaux)) |
| `icon` | Icône du portail |

:::tip[Visible sur tout le réseau]
Déclarer un service dans `etc/config.yaml` le rend visible et résolvable depuis
toutes les zones. Voir [Noms et résolution DNS](/fr/doc/admin-guide/understand/network/#noms-et-résolution-dns).
:::

{/* t:p 9ea0286a28faad321865258aca07f5a3557baabc8a2ee86bb783e1ca0fa40e1b */}
## Joindre un service de zone depuis l'extérieur

Un service **de zone** (non `global`) garde son URL de zone
`<domain>.<zone>.domain.tld` et ne répond normalement que sur le LAN, les autres
zones et le tailnet. Certains services sont en plus marqués `externalAccess` dans
le registre du framework (`dnf/config/modules.nix`) : le **HCS les publie alors
vers l'extérieur** tout en conservant l'URL de zone.

Routage : `internet → DNS public → HCS → (tailnet) → passerelle de zone →
backend`. Le HCS termine le certificat TLS public et relaie la requête vers la
passerelle de zone, dont le propre vhost gère le [SSO](/fr/doc/admin-guide/operate/sso/)
et le backend. La résolution interne est inchangée (directe vers l'hôte/la
passerelle).

| Exposer un service… | Drapeau | Résultat |
|---|---|---|
| Publiquement, tout le réseau | `global: true` (par service, `config.yaml`) | `<domain>.domain.tld`, sans zone, servi par le HCS |
| Publiquement, en gardant l'URL de zone | `externalAccess` (registre) | `<domain>.<zone>.domain.tld`, fronté par le HCS |

:::caution[Deux pré-requis]
- **DNS public** : `<domain>.<zone>.domain.tld` (ou le joker
  `*.<zone>.domain.tld`) doit résoudre vers l'**IP publique du HCS** chez votre
  registrar — le HCS en a aussi besoin pour obtenir le certificat ACME.
- Un service marqué `proxy.isInternal` (LAN uniquement, ex. `adguardhome`,
  `searx`) **ne peut pas** être `externalAccess` : le build échoue sur une
  assertion.
:::

{/* t:p 0d578dac029edb37f5e7dcbe26c7666da0fe961aac3e4dab6b40ef2bec38651f */}
## Le catalogue

| Catégorie | Services |
|---|---|
| Authentification | `idm` (Kanidm), `vaultwarden` |
| Fichiers & cloud | `nextcloud`, `oxicloud`, `immich`, `garage`, `minio`, `nfs` |
| Communication | `matrix`, `element`, `jitsi-meet`, `turn` |
| Médias & loisirs | `jellyfin`, `mealie`, `geneweb`, `stk` |
| Productivité | `outline`, `docs`, `searx` |
| IA | `ai` (Open WebUI + Ollama) |
| Réseau | `dnsmasq`, `adguardhome`, `headscale`, `tailscale`, `homepage` |
| Développement | `forgejo`, `harmonia`, `nix-cache` |
| Supervision & sauvegarde | `monitoring`, `loki`, `restic` |
| Système | `fail2ban`, `postfix`, `printing`, `audio`, `home-assistant` |

:::note[Référence complète]
Options détaillées de chaque service dans la
[référence des modules](/fr/ref/modules/service/).
:::

{/* t:p 5af71e667db357c0778a37eac1de0c4dbbe9db7f4ff0dd5a4c8a0e01f13a3e4b */}
## Portail et accès

- **Portail** : le service `homepage` dresse une page d'accueil des services, par zone.
- **Authentification unique** : la plupart des services passent par le [SSO Kanidm](/fr/doc/admin-guide/operate/sso/).
- **Local ou global** : un service reste dans sa zone, sauf `global: true` qui l'expose publiquement.

{/* t:p 83ad1db1dcf5c83ed4fdbae4d3735258ea074bb7bad2554821c8f564502b0eaa */}
## Services à version épinglée

Quelques services ne peuvent pas suivre la version par défaut de nixpkgs&nbsp;:
leur éditeur interdit de sauter une version majeure. DNF épingle donc le paquet
dans le module, et la montée se fait **une majeure à la fois**.

| Service | Option | Contrainte amont |
|---|---|---|
| `idm` (Kanidm) | `services.kanidm.package` | Une seule version maintenue, fin de vie 30 jours après la sortie de la suivante |
| `nextcloud` | `services.nextcloud.package` | 33 → 34 possible, 33 → 35 impossible |

:::caution[Contrôler la version en place avant de monter]
Épingler la majeure suivante alors que l'instance n'y est pas encore prête fait
échouer la migration au démarrage. Interroger d'abord le service lui-même&nbsp;:
`kanidmd domain upgrade-check` pour Kanidm, `nextcloud-occ status` pour
Nextcloud.
:::

Le signal d'une montée à planifier est un `warning` à l'évaluation, émis dès que
la version épinglée est dépréciée. La procédure détaillée de Kanidm, transposable
aux autres, est décrite en fin de page
[SSO et identités](/fr/doc/admin-guide/operate/sso/).

{/* t:p bcea1ed7430dadaade6fadfdc8dfc49af5c67e44a4ade5a633d95b1255ed3195 */}
### Sauvegarder avant de monter

Ces migrations réécrivent le schéma de la base au premier démarrage de la
nouvelle version, sans retour arrière. La sauvegarde préalable n'est pas une
précaution de confort.

Kanidm stocke en SQLite, donc **service arrêté**&nbsp;:

```sh
sudo systemctl stop kanidm
sudo install -d -o kanidm -g kanidm /var/lib/kanidm/pre-upgrade
sudo cp -a /var/lib/kanidm/kanidm.db* /var/lib/kanidm/pre-upgrade/
sudo kanidmd database backup /var/lib/kanidm/pre-upgrade/dump.json -c /etc/kanidm/server.toml
sudo chown -R kanidm:kanidm /var/lib/kanidm
sudo systemctl start kanidm
```

Le `*` est essentiel&nbsp;: le journal d'écriture `kanidm.db-wal` survit à
l'arrêt du service et porte les dernières transactions.

Nextcloud stocke en PostgreSQL, donc **sans interruption**&nbsp;: `pg_dump -Fc`
prend un instantané transactionnel cohérent sur une base vive.

```sh
sudo install -d -o nextcloud -g nextcloud -m 0750 /var/lib/nextcloud/pre-upgrade
sudo -u postgres pg_dump -Fc nextcloud > /var/lib/nextcloud/pre-upgrade/nextcloud.dump
sudo cp -a /var/lib/nextcloud/config/config.php /var/lib/nextcloud/pre-upgrade/
sudo chown -R nextcloud:nextcloud /var/lib/nextcloud/pre-upgrade
```

Ne pas omettre `config.php`&nbsp;: c'est lui qui porte le numéro de version
installée, sans lequel une base restaurée ne repart pas.

:::tip[Relire le dump avant de déployer]
Un dump illisible ne se découvre jamais au bon moment. `pg_restore -l` liste son
contenu sans rien restaurer&nbsp;:

```sh
sudo -u nextcloud pg_restore -l /var/lib/nextcloud/pre-upgrade/nextcloud.dump | wc -l
```
:::
