---
title: Utilisateurs et identités
sidebar:
  label: Utilisateurs
  order: 6
lang: fr
---

import { Steps } from '@astrojs/starlight/components';

{/* t:main */}
{/* t:h a16a904ba4cebc0eb0b300d71e8fdf2acdb7a6b7b8b5ee282b9fb96b6d721679 */}

{/* t:p f4e56a74e962230e926f93991fa077d96bd0f6018dfd04ddc8bb0c631bf3132d */}
Cette page décrit le compte déclaré dans `etc/config.yaml`&nbsp;: déclaration,
hôtes, mot de passe de session, SSH.

:::tip[Créer un compte de bout en bout]
Types de comptes (Linux, Services, ami Matrix) et parcours complets (non
technique ou technique)&nbsp;: voir
[Créer un compte utilisateur](/fr/doc/admin-guide/operate/onboarding/).
:::

{/* t:p 1156a0686209d54e9f12811173f61e73c611eb3ec899c9961192361308eab2dc */}
## Créer un compte Linux (POSIX)

Les utilisateurs sont déclarés **une fois** dans `etc/config.yaml`, puis
**rattachés** aux hôtes où ils doivent ouvrir une session. Le même compte peut
exister sur plusieurs machines.

{/* t:p 875f3ee8246831006547317554684773380c9a7fdc858a07ebba456a2770e30e */}
### Déclarer un utilisateur

```yaml
# etc/config.yaml
users:
  alice:
    uid: 1000
    name: "Alice Martin"
    email: "alice@domain.tld"
    profile: "advanced"
    groups: ["global", "idm-devs"]
```

| Champ | Rôle |
|---|---|
| *(clé)* | Login du compte (ex. `alice`) |
| `uid` | Identifiant numérique stable |
| `name` | Nom affiché |
| `email` | Adresse (sert aussi à l'identité SSO) |
| `profile` | [Profil utilisateur](/fr/doc/admin-guide/understand/user-profiles/) (environnement) |
| `groups` | Groupes (accès réseau et services) |

{/* t:p f93adb95d0c4124c98ae91d5835b63faf346221b89bc26b732dea3d70af6ffd4 */}
### Rattacher à un hôte

Sous un hôte, `users` liste les **logins existants** autorisés à s'y connecter :

```yaml
# etc/config.yaml
hosts:
  poste-alice:
    profile: "desktop"
    users: ["alice"]
```

:::tip[Via un groupe]
Autre possibilité : rattacher un utilisateur à un hôte grâce à un groupe commun déclaré dans la liste `groups` commune.
:::

{/* t:p 566263d5256294795aca42d3f61a7a982d58982ddad68b53c267d5382b2f515e */}
### Déployer les modifications

Toute modification de `etc/config.yaml` suit le **même cycle** :

<Steps>

1. **Décrire le changement**

   Créer ou modifier l'utilisateur et ses postes dans `etc/config.yaml`.

2. **Régénérer et nettoyer le code**

   ```sh
   just clean       # generate + fix + format
   ```

3. **Valider dans git**

   ```sh
   just commit "user(new): alice"
   ```

4. **Déployer**

   ```sh
   just apply poste-alice     # crée le compte sur l'hôte
   ```

</Steps>

:::caution[Toujours committer avant de déployer]
`just clean` réécrit `var/generated/`. Sans `clean` + `commit`, le
déploiement part d'un état incohérent ou non versionné.
:::

{/* t:p e3a147302d14e2e73b35651a82628b73004bdfcfb0405b111e2fbffcbfca7b06 */}
### Définir le mot de passe

<Steps>

1. **Définir l'empreinte**

   ```sh
   just passwd alice
   ```

2. **Valider dans git**

   ```sh
   just commit "chore(user): alice password"
   ```

3. **Déployer**

   ```sh
   just apply @user-alice,hcs
   ```

</Steps>

:::tip[Un compte n'a pas de mot de passe tant qu'on ne lui en donne pas]
`just passwd <login>` est ce qui ouvre la session ; `just passwd-default`
n'enregistre que le mot de passe de référence du parc. Voir
[Secrets](/fr/doc/admin-guide/operate/secrets/).
:::

:::note[Redéfinition de mot de passe (todo)]
Pour le moment il n'est pas possible pour un utilisateur de redéfinir son
mot de passe POSIX pour l'ensemble du parc.
:::

{/* t:p 82e591259013b8ec44c9f0e307b03860ec5e9853c0dc6affaa993da145a7e023 */}
### Accès aux services et à Matrix

- Le compte Kanidm est créé au déploiement du HCS, sans identifiant.
- Le compte Matrix se crée à la première connexion dans Element.
- Identifiants et coffre-fort&nbsp;: suivre l'un des deux [parcours de création](/fr/doc/admin-guide/operate/onboarding/#choisir-un-parcours).

{/* t:p 933885347c28a0e97bb028dbb55fad18f18efa9daf282a64ef33790217809ce9 */}
## Accès SSH

L'accès SSH est **par clé uniquement** et **entièrement déclaratif**, sur toute
la flotte :

- mot de passe et clavier-interactif désactivés (`darkone.system.core`) ;
- seules les clés déclarées comptent
  (`users.users.<login>.openssh.authorizedKeys`) : `~/.ssh/authorized_keys`
  n'est plus lu ;
- un compte sans clé déclarée ouvre sa session locale, jamais SSH.

:::note[La forge fait exception]
Le compte `forgejo` sert le `git push` de tous les dépôts : ses clés sont
celles que chaque utilisateur dépose dans l'interface web de la forge, pas
celles de la configuration NixOS. `sshd` les lui demande par
`AuthorizedKeysCommand`, et non par un fichier. Une clé s'ajoute donc dans
Forgejo (**Paramètres → Clés SSH / GPG**), jamais dans `config.yaml`.
:::

:::caution[Plus d'amorçage par ssh-copy-id]
Sans canal mot de passe ni fichier `~/.ssh/authorized_keys`, `ssh-copy-id` n'a
plus ni porte d'entrée ni cible. Toute clé passe par la configuration puis
`just apply`. Gardez un accès de secours (console, ou sshd d'initrd LUKS sur le
port 2222) avant de déployer sur un hôte distant.
:::

{/* t:p d5b50329c05599139e966e6f3e3280df40356023c90d591921e3462adfa308b4 */}
### Interdire SSH à un groupe

Les `groups` de `config.yaml` sont **logiques** : ils ne deviennent pas des
groupes Unix, `DenyGroups` ne verrait donc rien. DNF les traduit en `DenyUsers`,
hôte par hôte :

```nix
# Défaut : les comptes invités gardent leur session locale, pas SSH.
darkone.user.build.sshDeniedGroups = [ "guests" ];
```

{/* t:p 201b895a3fe4bc096c9396e3303466b4147f3dae7ccc53c7b49e2111e7cd37f5 */}
## Identités et SSO

Au-delà du compte système, l'utilisateur dispose d'une **identité unique**
(Kanidm) qui ouvre les services du réseau.

- Les `groups` pilotent l'accès aux services.
- `idm-admins` / `idm-devs` sont les groupes d'administration et de développement.
- Le détail : [SSO et identités](/fr/doc/admin-guide/operate/sso/).

:::note[Créer ou réinitialiser un compte]
Voir [Créer un compte utilisateur](/fr/doc/admin-guide/operate/onboarding/) et
[Réinitialiser un mot de passe](/fr/doc/how-to/admin/reset-user-password/).
:::

{/* t:p d929a47941fb703dd34d9ac49a678dc511433d466b60b6ab033b9f661944261d */}
## Désactiver un compte

- **Sur un hôte** : retirer le login de la liste `users` de l'hôte.
- **Partout** : retirer l'entrée sous `users:`.
- **Accès services** : retirer l'utilisateur des `groups` coupe l'accès SSO.

Dans tous les cas, appliquer ensuite le cycle `just clean` → `just commit` →
`just apply` sur les hôtes concernés.
