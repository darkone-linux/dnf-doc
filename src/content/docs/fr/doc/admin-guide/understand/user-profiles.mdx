---
title: Les profils d'utilisateurs
sidebar:
  label: Profils d'utilisateurs
  order: 4
lang: fr
---

{/* t:main */}
{/* t:h 7a669dd2bc1b1e4ce3d39840716aad221372eaa799c7dfc06ae6e844a631286e */}

{/* t:p 4f2fbb0d6953859f115566091021378007463fd49f7acb33ed23ef58601ba398 */}
Un **profil d'utilisateur** définit l'environnement [Home Manager](https://github.com/nix-community/home-manager)
d'un compte : applications, bureau, réglages. On le choisit avec `profile:` dans
`etc/config.yaml`. Comme les hôtes, les profils s'héritent.

{/* t:p 8f33ee343397f6c613277e5c89ae8a372fd7bded177c9fde260419c3274f4217 */}
## L'héritage

```d2
style.fill: transparent
direction: down

minimal: minimal
normal: normal
advanced: advanced
admin: admin
nixadmin: nix-admin
student: student
teenager: teenager
gamer: gamer
child: child
baby: baby
umi: umi

minimal -> normal
minimal -> teenager
minimal -> gamer
minimal -> child
minimal -> baby
normal -> advanced
normal -> umi
advanced -> admin
advanced -> student
admin -> nixadmin
```

{/* t:p 9541bd4a9788a4ff38b843f98cd81df90bfc2cdc5b22807aebe6e921b891d286 */}
## Les profils

| Profil | Pour qui | Hérite de |
|---|---|---|
| `minimal` | Base de tout profil | |
| `normal` | Utilisateur standard (bureau) | `minimal` |
| `advanced` | Développeurs, informaticiens | `normal` |
| `umi` | Utilisateur à saisie multimodale (toucher, voix, regard) | `normal` |
| `admin` | Administrateur réseau | `advanced` |
| `nix-admin` | Administrateur du parc (outils Nix) | `admin` |
| `student` | Étudiant | `advanced` |
| `teenager` | Adolescent | `minimal` |
| `gamer` | Joueur | `minimal` |
| `child` | Enfant (icônes techniques masquées) | `minimal` |
| `baby` | Très jeune enfant | `minimal` |

:::tip[Le profil `umi` va par paire]
Il n'a de sens que sur un hôte en profil `umi`, qui fournit la session X11 et
la prise en charge des périphériques de saisie (traqueur oculaire, injection
d'événements). Voir
[Accessibilité](/fr/doc/admin-guide/operate/accessibility/).
:::

:::tip[Côté utilisateur]
Une présentation orientée usage figure dans le
[guide de l'utilisateur](/fr/doc/user-guide/understand/profiles/).
:::

## Le bureau GNOME

| Élément | Profils | Comportement |
|---|---|---|
| Paramètres Qt5 / Qt6, manuel NixOS | tous | Icônes masquées (manuel accessible via `nixos-help`) |
| Notification « Soutenir GNOME » | tous | Désactivée |
| Visionneur de documents | tous | Papers, application PDF par défaut (remplace Evince) |
| Visionneur d'images | tous | Geeqie par défaut, si le module `imagery` est actif |
| Extensions GNOME | non techniques | Icône masquée |
| Terminal | non techniques | Console, absente du dash, présente dans la grille d'applications |
| Terminal | techniques | Ghostty dans le dash, si `darkone.home.advanced.enableTools` est actif |

:::note[Dash verrouillé]
La liste des favoris (`favorite-apps`) est verrouillée pour tout l'hôte&nbsp;:
un profil technique sans Ghostty n'a donc pas non plus Console dans son dash.
:::

{/* t:p a457e1b1c9e9a250f11800283e6121e4738416e7bd4434b723a7be4fb49a4a57 */}
## Affecter un profil

```yaml
# etc/config.yaml
users:
  alice:
    uid: 1000
    name: "Alice"
    profile: "nix-admin"
    groups: ["global"]
```

:::note[Personnaliser]
On crée ou surcharge un profil dans `usr/home/profiles/<nom>/`. Les briques
réutilisables sont les [modules Home Manager](/fr/ref/modules/home/).
:::
