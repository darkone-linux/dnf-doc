---
title: Sécurité et durcissement
sidebar:
  label: Sécurité
  order: 1
lang: fr
---

{/* t:main */}
{/* t:h 90bed1dbb460174673d09d5a53fca89bf3510d9738c0ebd7355c58aec8b6c9a2 */}

{/* t:p 383c33987c3ef13705a2d58d96ff1b596c9efac28513b00362a69ed842b3784c */}
Le module `darkone.system.security` applique progressivement les recommandations
de durcissement [ANSSI BP-028](https://cyber.gouv.fr/publications/recommandations-de-securite-relatives-un-systeme-gnulinux)
(GNU/Linux). Chaque hôte choisit un **niveau** et une **catégorie** ; les règles
adaptées s'activent automatiquement.

:::danger[Module en développement]
Le durcissement ANSSI est **en cours de stabilisation**. Montez le niveau
**progressivement** et testez chaque hôte avant la production.
:::

{/* t:p a086756d1e56e2d100e8acba92f9009be188dc1493c45e20ba4baf51f4a47f3d */}
## Niveau et catégorie

```nix
darkone.system.security = {
  enable   = true;
  level    = "intermediary";   # minimal | intermediary | reinforced | high
  category = "server";         # base | client | server
};
```

| Niveau | Pour quoi |
|---|---|
| `minimal` | Socle commun, tout système (défaut) |
| `intermediary` | Recommandé pour la quasi-totalité des systèmes |
| `reinforced` | Systèmes sensibles ou multi-utilisateurs |
| `high` | Compétences et budget dédiés (recompilation noyau) |

| Catégorie | Pour quoi |
|---|---|
| `base` | Règles universelles, toujours appliquées (défaut) |
| `client` | Poste de travail (bureau, USB, session, verrouillage) |
| `server` | Serveur (réseau durci, journalisation, services exposés) |

:::note[Activation par règle]
Une règle s'active si le module est activé **et** que le niveau couvre sa
sévérité **et** que la catégorie correspond **et** qu'aucun tag d'exclusion ni
exception ne la vise.
:::

{/* t:p 52013b643a27b4e847977d6a759af6831122b4ca4ea81ec05d978217a1813bf5 */}
## Exclure un groupe de règles

Certaines règles cassent un usage légitime. On désactive un **groupe** par tag :

```nix
darkone.system.security.excludes = [ "needs-hibernation" "needs-jit" ];
```

| Tag | Désactive |
|---|---|
| `kernel-recompile` | Règles noyau custom (R15–R27) |
| `no-ipv6` | Désactivation IPv6 (R13, R22) |
| `no-mac` | MAC / AppArmor (R37, R45) |
| `needs-hibernation` | Conserve l'hibernation (portables) |
| `needs-jit` | Autorise le JIT (Java, .NET, V8, Wasm) |
| `needs-usb-hotplug` | Désactive USBGuard |

{/* t:p a1dac3f94cb3f13167a63dea41d74e17746213335a61d5f169d27b144fe7893f */}
## Déroger à une règle précise

Une règle unique se contourne avec une **justification obligatoire** :

```nix
darkone.system.security.exceptions = {
  R9.rationale = "Conteneurs rootless requis en développement.";
};
```

:::caution[Effets de bord]
Certaines règles gênent l'usage courant : `noexec /tmp` (R28), SMT désactivé
(R8), modules non signés (R18). Vérifiez les commentaires `sideEffects` du module
avant de monter le niveau en production.
:::

{/* t:p 5ca2399225ec22c276944381d8a9b4918a20d84f4c4c7179400f212b2c2083a9 */}
## MAC, pare-feu, fail2ban

- **MAC / AppArmor** (R37, R45) : confinement applicatif ; SELinux est
  structurellement non supporté sur NixOS (exceptions R46–R49 par défaut).
- **Pare-feu & réseau** : durcis par la catégorie `server` (module `network`).
- **fail2ban** : bannit les IP après tentatives répétées ; service à activer
  dans le [catalogue](/fr/doc/admin-guide/operate/services/).

{/* t:p b760b080d259ac3fdcd87ffda6369730305015387aef4c727ac50538104fb08e */}
## Confinement des services et amorçage

Deux règles demandent une décision explicite de l'administrateur :

- **Bac à sable systemd** (R52, R55, R63) : le confinement complet s'applique aux
  unités déclarées dans `darkone.security.services.hardenedUnits` — **vide par
  défaut**, car un durcissement aveugle casserait des services. On y ajoute les
  unités au cas par cas après test.
- **Amorçage et chiffrement** (R3, R5, C6) : le module **avertit** si le Secure
  Boot ou LUKS ne sont pas détectés, mais ne reconfigure pas le démarrage. Ces
  protections se déclarent dans la configuration disque de l'hôte (`disko`,
  `lanzaboote`).

```nix
darkone.security.services.hardenedUnits = [ "nginx" "gitea" ];
```

:::note[Référence des règles]
La cartographie complète des règles (R1–R80, C1–C12) figure dans le
[catalogue](/fr/doc/admin-guide/operate/security/) et la [référence des
modules](/fr/ref/modules/security/).
:::
