---
title: Modules Home Manager
sidebar:
  order: 10
  label: Home Manager
---

{/* t:translated-from en */}
{/* t:h a7451805e0edb96998b718b7d1beac5169a0126d9f6d9390ccd995c36df95f29 */}

{/* t:p d778eab736fb017fb48b64e9241a81df0712eb65a7bb5160d5120fd8fbf82912 */}
:::note
**Un module home manager** fonctionne avec les profils [home manager](https://github.com/nix-community/home-manager).
:::

{/* t:p 36b6c508392ab7b255a2b12448623593208e05129934d548b6efc61661c5df57 */}
### &#x1F3E0; darkone.home.advanced

Profil d'accueil pour les utilisateurs avancés (informaticiens, développeurs, administrateurs).

* **enable** `bool` Activer les fonctionnalités des utilisateurs avancés
* **enableTools** `bool` Divers outils pour les utilisateurs avancés
* **enablePhoneTools** `bool` Outils smartphone (scrcpy)
* **enableAdmin** `bool` Activer les fonctionnalités d'administrateur (réseau, outils os)
* **enableNixAdmin** `bool` Activer les fonctionnalités d'administration nix
* **enableDeveloper** `bool` Activer les outils pour développeur
* **enableEssentials** `bool` Outils fréquemment utilisés

```nix
darkone.home.advanced = {
  enable = false;
  enableTools = false;
  enablePhoneTools = false;
  enableAdmin = false;
  enableNixAdmin = false;
  enableDeveloper = true;
  enableEssentials = true;
};
```

{/* t:p a5e441f3c183fb2f15936a96c5e611e8f896620e006989a02ee60fa5470d9fc5 */}
### &#x1F3E0; darkone.home.ai

Outils de codage IA et utilitaires de support.

:::note[Drapeaux]
- `darkone.home.ai.enable` commutateur principal installe les paquets utilitaires
  (aichat, llm, fabric-ai, gh, lazygit, direnv, outils de qualité de code, etc.)
  et active le système de drapeau par agent ci-dessous.
- `darkone.home.ai.enable{Claude,OpenCode,Codex,Aider,Goose}` chacun
  installe et configure un agent de codage IA spécifique.
- `darkone.home.ai.preferLocal` lorsqu'il est combiné avec un hôte `darkone.service.ai`
  ollama local, les agents utilisent par défaut le modèle local au lieu des APIs cloud.
:::

:::tip[Câblage par hôte]
Ce module lit `osConfig.darkone.service.ai.enable` pour détecter un
hôte ollama local. Sur ces hôtes, `gollama`, la CLI `ollama` et
les valeurs par défaut du modèle local pour Aider / OpenCode sont activés automatiquement.

`programs.gh` est activé avec l'extension `github-copilot-cli`.
OpenCode obtient deux serveurs MCP (système de fichiers, fetch) via `npx -y`.
Claude Code applique une matrice d'autorisations allow/ask/deny et un hook de gouvernance RTK
sur les appels Bash. Son `settings.json` n'est PAS épinglé comme symlink du magasin
en lecture seule : le socle est fusionné dans un `~/.claude/settings.json` WRITABLE
à chaque commutation, afin que les plugins/modules additionnels
(claude-mem, graphify, ...) puissent être installés et gérés à la main par-dessus.
:::

* **enable** `bool` Activer les outils IA et les utilitaires de support
* **enableClaude** `bool` Claude Code CLI
* **enableOpenCode** `bool` Agent IA terminal OpenCode
* **enableCodex** `bool` CLI OpenAI Codex
* **enableAider** `bool` Programmation en pair IA Aider
* **enableGoose** `bool` Agent de codage IA Goose (Block Inc.)
* **preferLocal** `bool` Préférer les modèles locaux aux modèles cloud

```nix
darkone.home.ai = {
  enable = false;
  enableClaude = false;
  enableOpenCode = false;
  enableCodex = false;
  enableAider = false;
  enableGoose = false;
  preferLocal = false;
};
```

{/* t:p 0fabfb5339e05d605bca4e56694acf6244ae0c63d5b9fa84a7005fab56fe6689 */}
### &#x1F3E0; darkone.home.audio

Outils et effets audio.

Installe un lecteur audio (`vlc`) et l'encodeur MP3 (`lame`)
sans condition, puis superpose les éditeurs et les effets (`audacity`,
`easyeffects` avec le préset `easyeffects-fw16`) lorsque `enableTools` est
défini. La réduction de bruit en temps réel (`noisetorch`) est intentionnellement désactivée
car elle nécessite PulseAudio.

* **enable** `bool` Outils audio
* **enableTools** `bool` Outils audio / éditeurs (audacity, easyeffect, noisetorch...)

```nix
darkone.home.audio = {
  enable = false;
  enableTools = false;
};
```

{/* t:p ccba3aeb30aded9435afa145d2d81ea366f47acc6973791166ad3a872f9485e0 */}
### &#x1F3E0; darkone.home.education

Plusieurs paquets d'éducation graphiques.

* **enable** `bool` Collection de logiciels éducatifs
* **enableBaby** `bool` Logiciels éducatifs pour bébés (&lt;=6 ans)
* **enableChild** `bool` Logiciels éducatifs pour enfants (6-12 ans)
* **enableStudent** `bool` Logiciels éducatifs pour adolescents et adultes (&gt;=12 ans)
* **enableMath** `bool` Outils et applications mathématiques
* **enableMusic** `bool` Outils et applications musicales
* **enableScience** `bool` Outils et applications scientifiques
* **enableDraw** `bool` Outils et applications de dessin
* **enableLang** `bool` Outils et applications de langue
* **enableMisc** `bool` Outils et applications diverses (général, formation...)
* **enableComputer** `bool` Outils et applications informatiques (klavaro, etc.)

```nix
darkone.home.education = {
  enable = false;
  enableBaby = false;
  enableChild = false;
  enableStudent = false;
  enableMath = true;
  enableMusic = true;
  enableScience = true;
  enableDraw = true;
  enableLang = true;
  enableMisc = true;
  enableComputer = true;
};
```

{/* t:p e202c2e0e406911619242d888e19e0712c66acc344372d279c687929053926fc */}
### &#x1F3E0; darkone.home.games

Plusieurs jeux graphiques.

* **enable** `bool` Activer les jeux
* **enableBaby** `bool` Jeux pour bébés (&lt;=6 ans)
* **enableChild** `bool` Jeux pour enfants (6-12 ans)
* **enableTeenager** `bool` Jeux pour adolescents et adultes (&gt;=12 ans)
* **enable3D** `bool` Plus de jeux 3D
* **enableCli** `bool` Jeux en ligne de commande
* **enableStk** `bool` SuperTuxKart (uniquement)
* **enableMore** `bool` Plus de jeux (secondaires) dans chaque catégorie

```nix
darkone.home.games = {
  enable = false;
  enableBaby = false;
  enableChild = false;
  enableTeenager = false;
  enable3D = false;
  enableCli = false;
  enableStk = false;
  enableMore = false;
};
```

<hr/>

{/* t:p 8cc86b72cc50ff00f8cfc4576fb69487d652391b82246e554ecb33446336cdec */}
### &#x1F3E0; darkone.home.gnome

Ajustements Gnome pour home-manager.

Masque inconditionnellement le lanceur nu `xterm`, enregistre des
entrées `.desktop` soignées pour `scrcpy` et `scrcpy-console` lorsque le
package fait partie des `home.packages` de l'utilisateur, et (lorsque
`hideTechnicalIcons` est défini) masque les icônes de Paramètres,
Extensions, Imprimantes et File Roller pour les profils débutant/enfant.

:::caution[Signets NFS]
Ne pas déclarer `gtk.gtk3.bookmarks` depuis ce module — tant que les parts
home NFS sont actives, `home/profiles/minimal/nfs.nix` possède ce fichier
et l'écrase.
:::

* **enable** `bool` Activer les paramètres Gnome pour home-manager
* **hideTechnicalIcons** `bool` Masquer certaines icônes pour débutants / enfants / bébés

```nix
darkone.home.gnome = {
  enable = false;
  hideTechnicalIcons = false;
};
```

<hr/>

{/* t:p 66e29bc700c56c059782e370db0f691a83609c85f44233ef1b041e62f5537ba4 */}
### &#x1F3E0; darkone.home.imagery

Logiciels de traitement d'images.

Installe toujours la visionneuse légère (`geeqie`) et l'éditeur (`gimp`),
puis superpose les outils par audience : `enableBeginner` ajoute `pinta`,
`enablePro` ajoute `inkscape`/`krita`/`yed`, `enable3D` (avec
`enablePro`) ajoute `blender`, `enableCAD` ajoute `freecad` (de nixpkgs stable
pour éviter l'agitation Qt), et `enableCli` ajoute `imagemagick`/`jhead`.

* **enable** `bool` Création d'images
* **enablePro** `bool` Logiciels de traitement d'images supplémentaires pour les professionnels
* **enableBeginner** `bool` Logiciels de traitement d'images supplémentaires pour les débutants
* **enable3D** `bool` Logiciels 3D
* **enableCAD** `bool` Logiciels CAO
* **enableCli** `bool` Outils CLI (imagemagick, jhead...)

```nix
darkone.home.imagery = {
  enable = false;
  enablePro = false;
  enableBeginner = false;
  enable3D = false;
  enableCAD = false;
  enableCli = false;
};
```

{/* t:p 6ead6a8f78c5432c5d37e53fc79876316270cdadcd91b4b5c6e702475e4d7b67 */}
### &#x1F3E0; darkone.home.mime

Module types Mime DNF. (en cours)

:::note
Ce module définit les applications par défaut selon le profil utilisateur. Par exemple, éditeur de markdown pour...
- Débutant (par défaut) : appostrophe, éditeur de texte gnome...
- Avancé : zed, vim, vscode...
:::

* **enable** `bool` Activer la mise à jour des applications par défaut DNF

```nix
darkone.home.mime.enable = false;
```

{/* t:p 4d5ba9efe2876512ee41d5b3d5f76ba1f24686bccc1d1c9cbf6878ec3736ca31 */}
### &#x1F3E0; darkone.home.music

Applications graphiques de création musicale et sonore.

Catégorisées par audience : `enablePro` (ardour, reaper, renoise,
rosegarden), `enableCreator` (decibels, hydrogen), `enableScore`
(musescore, muse-sounds-manager), `enableFun` (mixxx, mousai),
`enableCli` (mpg123, cmus, lilypond), `enableEasy` (gnome-music vs.
audacious), `enableMpd` (daemon MPD, ncmpcpp, mpdris2) et
`enableDev` (lilypond).

:::note[Bibliothèque musicale consciente NFS]
Lorsque l'hôte est un client NFS et que le réseau expose un service `nfs`
dans la même zone, MPD lit son répertoire musical à partir du montage NFS
(`/mnt/nfs/homes/$USER/Music`). Sinon, il revient à la
valeur `srv-dirs.homes`, puis à `~/Music`.
:::

* **enable** `bool` Module home de création musicale
* **enablePro** `bool` Outils difficiles pour les professionnels (rose, ardour...)
* **enableFun** `bool` Outils audio amusants (mixxx...)
* **enableCli** `bool` Outils audio de ligne de commande (mpg123, cmus, moc...)
* **enableDev** `bool` Logiciels audio pour les développeurs (lilypond...)
* **enableMpd** `bool` Daemon et lecteurs MPD (mpd, ncmpcpp...)
* **enableEasy** `bool` Outils faciles au lieu d'efficaces (gnome-music vs audacious...)
* **enableScore** `bool` Logiciels de partition (musescore...)
* **enableCreator** `bool` Outils de création pour les débutants (lmms, hydrogen...)

```nix
darkone.home.music = {
  enable = false;
  enablePro = false;
  enableFun = false;
  enableCli = false;
  enableDev = false;
  enableMpd = false;
  enableEasy = false;
  enableScore = false;
  enableCreator = false;
};
```

{/* t:p fd6d1f2d551335bdea41eed6b4bc0457dfa401b4b03c69fda83dc5eb272f20a1 */}
### &#x1F3E0; darkone.home.office

Outils courants pour le bureau de travail.

* **enable** `bool` Paquets utiles par défaut
* **enableMore** `bool` Plus de paquets alternatifs
* **enableUnsafeFeatures** `bool` Fonctionnalités pour utilisateurs avancés non enfants
* **enableUBlock** `bool` Active le plugin ublock
* **enableTools** `bool` Petits outils (gnome) (iotas, dialect, etc.)
* **enableProductivity** `bool` Applications de productivité (obsidian, gestion du temps, projets, etc.)
* **enableCommunication** `bool` Outils de communication
* **enableOffice** `bool` Paquets bureautiques (libreoffice, huntspell, polices...)
* **enableFirefox** `bool` Active firefox
* **enableLibreWolf** `bool` Active LibreWolf (alternative à firefox)
* **enableChromium** `bool` Active chromium
* **enableBrave** `bool` Active le navigateur Brave
* **enableEmail** `bool` Paquets de gestion d'e-mails (thunderbird)
* **enableSecurity** `bool` Outils de sécurité (keepass)
* **enableCalendarContacts** `bool` Calendrier GNOME, contacts et applications associées
* **enableEssentials** `bool` Outils essentiels
* **huntspellLang** `str` [Langue Huntspell](https://mynixos.com/nixpkgs/packages/hunspellDicts)
* **enableElementAutoStart** `bool` Démarre automatiquement Element Desktop à la connexion lorsqu'un serveur Matrix local est présent
* **enableFractalAutoStart** `bool` Démarre automatiquement Fractal à la connexion lorsqu'un serveur Matrix local est présent (sur option, voir les mises en garde ci-dessous)
* **nextcloud.enable** `bool` Installe le client de bureau Nextcloud, pré-lié à l'instance du réseau
* **nextcloud.server** `nullOr str` Instance Nextcloud à laquelle le client se lie. `null` choisit celle déclarée
  sur le réseau (cette zone en premier). À définir pour cibler une instance locale ou
  externe à la place.
* **nextcloud.enableAutoStart** `bool` Démarre le client Nextcloud en arrière-plan à la connexion
* **nextcloud.enableWebdav** `bool` Livre `nextcloud-webdav-login`, un assistant à usage unique qui authentifie
  via le navigateur (SSO Kanidm) et enregistre le résultat comme compte en ligne
  GNOME&nbsp;: fichiers via WebDAV dans le gestionnaire de fichiers, plus calendrier
  et contacts dans Evolution. Nécessaire car Nextcloud refuse de créer
  un mot de passe d'application depuis son interface web pour un utilisateur OIDC.

  L'assistant affiche aussi le mot de passe d'application, afin que tout autre
  client WebDAV puisse être pointé vers le même compte.

  :::note[GNOME uniquement]
  Le compte est écrit là où GNOME Online Accounts le lit, ce que
  `darkone.graphic.gnome` active de lui-même dès que le réseau
  porte un cloud. Sur un autre bureau, le mot de passe affiché reste utilisable.
  :::

  Ignoré lorsque `darkone.home.office.nextcloud.enable` est désactivé.
* **nextcloud.syncDir** `nullOr str` Dossier local pré-rempli dans l'assistant de configuration, relatif à `$HOME`.

  :::caution[Aucune synchronisation par défaut]
  Laissé à `null` exprès. L'assistant s'arrête alors sur sa page de dossier et
  l'utilisateur décide quoi, le cas échéant, synchroniser. Le définir ignore
  cette page.
  :::

```nix
darkone.home.office = {
  enable = false;
  enableMore = false;
  enableUnsafeFeatures = false;
  enableUBlock = false;
  enableTools = false;
  enableProductivity = false;
  enableCommunication = false;
  enableOffice = false;
  enableFirefox = false;
  enableLibreWolf = false;
  enableChromium = false;
  enableBrave = false;
  enableEmail = false;
  enableSecurity = false;
  enableCalendarContacts = false;
  enableEssentials = true;
  huntspellLang = "en-us";
  enableElementAutoStart = true;
  enableFractalAutoStart = false;
  nextcloud.enable = detectedNextcloud != null;
  nextcloud.server = "https://cloud.example.com";
  nextcloud.enableAutoStart = true;
  nextcloud.enableWebdav = true;
  nextcloud.syncDir = "Nextcloud";
};
```

<hr/>

{/* t:p 6b680883b6e70004f1c4282bd8d309bca0c375c25be9bace4ec444e36705edab */}
### &#x1F3E0; darkone.home.umi

Unified Multimodal Input (UMI)

Écran tactile + voix + regard (Talon démarrage automatique + Onboard).

:::note[Complémentaire au profil hôte umi]
L'hôte doit fournir udev/uinput et la session Cinnamon X11 (voir
`darkone.host.umi`, qui définit UMI). Ce module configure côté utilisateur : activation du
clic par stationnement (`enableDwell`), clavier Onboard ancré toujours visible avec
prédiction de mots et modificateurs persistants, et démarrage automatique de la session Talon.
:::

:::tip[Scripts communautaires]
Définir `communityScripts` sur un arbre github:talonhub/community récupéré pour obtenir
le contrôle vocal et du pavage oculaire (zoom souris, pop click) de manière déclarative.
:::

:::note[Doubles schémas]
La session UMI est Cinnamon (org/cinnamon/*) mais les clés GNOME
(org/gnome/*) restent synchronisées : mousetweaks lit le schéma d'accessibilité GNOME,
et ils servent de repli si l'utilisateur se retrouve un jour dans une session GNOME.
Exception : les clés verrouillées par le module DNF gnome ne sont jamais répliquées ici,
une écriture sur une clé verrouillée interrompt l'ensemble du `dconf load` de home-manager.
Le verrouillage d'écran reste activé côté GNOME (verrouillé par le module DNF gnome) ;
`idle-delay = 0` évite de le déclencher, et la session Cinnamon a son
propre verrouillage entièrement désactivé.
:::

:::caution[Trousseau de connexion : deux modes, décidés par l'hôte]
Une session de connexion automatique ne saisi aucun mot de passe, donc PAM n'a rien à transmettre
à gnome-keyring et chaque application utilisant des secrets affiche une invite gcr que l'utilisateur
UMI ne peut pas répondre. La façon d'éviter cela dépend de l'hôte :

- **Hôte non chiffré** : le trousseau de connexion est initialisé avec un mot de passe vide,
  que gnome-keyring stocke en texte clair et déverrouille de lui-même (il essaie
  un mot de passe vide avant de proposer une invite). Tout trousseau préexistant protégé par mot de passe
  est supprimé — sa phrase de passe est impossible à saisir ici, il ne peut donc que
  produire l'invite en permanence. La connexion automatique accorde déjà l'ensemble de la session à
  quiconque démarre la machine, il n'y a donc pas de perte de confidentialité réelle.
- **Hôte chiffré** (`darkone.system.luks.volumes` non vide) : rien n'est
  initialisé. La phrase de passe saisie au démarrage est mise en cache dans le trousseau noyau et
  transmise à gnome-keyring par `pam_gdm`, le trousseau reste donc chiffré avec
  elle. C'est le seul mode qui protège les secrets au repos.

Attention, hôtes chiffrés uniquement : la rotation de la phrase de passe LUKS (valeur
sops modifiée, `luks-passphrase-sync`) laisse le trousseau sur l'ancienne — GNOME
signale ensuite « Le mot de passe que vous utilisez pour vous connecter à votre ordinateur ne
correspond plus à celui de votre trousseau de connexion ». Supprimer
`~/.local/share/keyrings/login.keyring` et se reconnecter.
:::

* **enable** `bool` Configuration d'entrée multimodale UMI (Talon + Onboard)
* **enableTalonAutostart** `bool` Démarrer Talon à la connexion de la session (talon doit être dans PATH).
* **communityScripts** `nullOr package` Arbre talonhub/community déployé dans ~/.talon/user/community.
* **enableDwell** `bool` clic par stationnement : cliquer en reposant le pointeur. Désactivé par défaut — il n'est
  utilisable qu'avec un tracker oculaire, et avec une souris il se déclenche sur ce
  sur quoi le pointeur était positionné (le menu d'accessibilité activant le zoom ou le clavier
  à l'écran, typiquement)
* **dwellTime** `float` Délai du clic par stationnement en secondes.

```nix
darkone.home.umi = {
  enable = false;
  enableTalonAutostart = true;
  communityScripts = null;
  enableDwell = false;
  dwellTime = 1.2;
};
```

<hr/>

{/* t:p c4e7ccd302eeb7b1f711e952c7b240e65376ef7ae3efef2d060a20669491507b */}
### &#x1F3E0; darkone.home.video

Outils et applications vidéo.

Installe par défaut le lecteur (`celluloid`, interface GTK4/libadwaita au-dessus de mpv)
et conditionne les autres par audience : `enableTools` (ffmpeg, mlt, vlc,
video-trimmer, parabolic), `enableEditing` (kdenlive + shotcut depuis stable
nixpkgs), `enableCreator` (OBS Studio avec `obs-backgroundremoval`,
`obs-vkcapture`, etc.), `enableUnfree` (davinci-resolve depuis stable),
et `enableAlternative` (mpv).

:::caution[Pas GNOME Showtime]
Showtime 50 se bloque sur certains fichiers — `get_state(CLOCK_TIME_NONE)` sur le
thread principal, [GNOME/showtime#299](https://gitlab.gnome.org/GNOME/showtime/-/work_items/299).
Le processus bloqué conserve le nom D-Bus `org.gnome.Showtime`, donc chaque
lancement ultérieur est activé vers celui-ci et ne fait rien silencieusement :
ni fenêtre, ni log.
Non corrigé en amont à la version 50.0.
:::

* **enable** `bool` Module maison de création et d'outils vidéo
* **enableTools** `bool` Outils vidéo pour professionnels
* **enableEditing** `bool` Outils de montage vidéo (kdenlive…)
* **enableCreator** `bool` Outils de création vidéo (obs…)
* **enableUnfree** `bool` Applications vidéo non libres (davinci…)
* **enableAlternative** `bool` Applications vidéo alternatives (mpv…)

```nix
darkone.home.video = {
  enable = false;
  enableTools = false;
  enableEditing = false;
  enableCreator = false;
  enableUnfree = false;
  enableAlternative = false;
};
```

<hr/>
