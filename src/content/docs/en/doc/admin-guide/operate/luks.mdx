---
title: Disk Encryption (LUKS)
sidebar:
  label: LUKS
  order: 2.7
lang: en
---

{/* t:translated-from fr */}
{/* t:h 254e857338e9595c03a7667fac194bc95e30f99e6371ec6670586678427032f6 */}

{/* t:p a5485be3abd7f6b20c2f761155ca3d85056aa91a4880fe27ea0a9b196e63e9f5 */}
An encrypted host (LUKS2 volume declared in its disko configuration) protects
its data in case of theft or disposal, but asks for a passphrase at every
boot. DNF industrializes this point of friction with two guarantees:

- **several unlocking methods coexist** at all times, so losing one method
  (forgotten passphrase, misplaced YubiKey) never locks you out;
- **the boot prompt is answered remotely**, from the administration
  workstation, with no screen or keyboard attached to the host, and that from
  the **very first boot** that follows the installation.

{/* t:p 1434fdb9746b96fef619b8a0830b9735a836e82e4646e214d53362a76e1337c1 */}
## Unlocking methods

Each method occupies its own keyslot in the LUKS2 header. All are valid at the
same time, on every encrypted volume of the host.

| Method | Where it lives | Role |
|---|---|---|
| **Shared passphrase** | sops → `luks-passphrase` | A single one for the whole fleet, authorizes keyslot management. It is also the one that **formats** the disk |
| **Host passphrase** | sops → `luks/<host>/passphrase` | To be entrusted to the machine's user without exposing the fleet |
| **FIDO2 YubiKeys** | Derived from the physical key | Touch the key at boot, nothing to type |

:::note[Why two passphrases and not one]
They are not meant for the same people: the shared one is the fleet
administrator's key, the per-host one is entrusted to the user of a single
machine. And because there are two of them, rotating one remains **authorized
by the other**: the synchronization service never ends up without a way to act
on the keyslots, it adds the new slot before killing the old one.
:::

:::note[There is no installation passphrase anymore]
`just install` formats the volume with the shared passphrase
(`nixos-anywhere --disk-encryption-keys`): the formatting keyslot **is** the
shared keyslot. Nothing to remember from the installation, nothing to retype
afterwards. A host installed before that switch is detected by `just luks <host>`,
which then asks for its installation passphrase one last time to seed the
shared slot.
:::

:::note[Nothing to declare in the Nix configuration]
The `darkone.system.luks` module is active by default but **totally inert** as
long as the host does not appear in the manifest `usr/secrets/luks.json` **and**
does not declare a LUKS volume in its disko. Existing encrypted hosts do not
change until `just luks <host>` has been run.
:::

{/* t:p 8b1d0322647e139c105987f1f78759c3b61ecf4327baf619a13cd3e381c804f4 */}
## Provisioning a host

```sh
just luks <host>            # provisions (or converges) the encryption
just luks <host> pre        # offline half, without touching the machine
just luks <host> passwd     # rotates the passphrase per host
just luks <host> show       # audit: keyslot table + compliance
just luks <host> '' <ip>    # targets an explicit IP (host not yet resolved)
```

The recipe is **idempotent**: each run converges and only requests what is
missing. It is called automatically at two moments, and you rarely have to run
it yourself:

| Called by | Action | What it does |
|---|---|---|
| `just install <host>` | `pre` | Before the build, therefore before installation |
| `just configure <host>` | (default) | After the first boot |

Without a LUKS volume in disko, this is a logged no-op.

The **`pre`** phase never touches the machine. It prepares what must exist
*before* the system is built:

1. the **shared passphrase** in sops if absent (asked only once for the whole
   fleet);
2. the **per-host passphrase** in sops if absent, **generated at random and
   without asking anything** (see the box below);
3. the **SSH host key of the initrd**, generated locally and stored in sops
   (`luks/<host>/initrd-key`, base64 encoded);
4. the host's entry in the **public manifest** `usr/secrets/luks.json`
   (versioned in clear, it contains no secret): it is this entry that
   activates the module.

:::note[Why the per-host passphrase is never asked at installation]
It is used to entrust a station's unlocking to its user without giving them
the key to the whole fleet. But at formatting time we still do not know who
will use the machine: the answer was always "empty". And an interactive
question in the middle of an installation is exactly what breaks a remote
installation.

It is therefore drawn at random and kept in sops alone. The slot keeps its
purpose — it is a second way to open the volume, and the fallback of
`just unlock`. The day someone really has to type a passphrase on that
machine, `just luks <host> passwd` gives them a chosen one, and
`luks-passphrase-sync` swaps the slot at the next `apply`.
:::

The default phase adds what needs the machine:

5. re-lays the **initrd key** from sops if it is missing on the target (it
   never regenerates a new one, that would invalidate the one laid by the
   installation);
6. on a **gateway**, records the current **WAN IP** in the manifest
   (fallback of `just enter` and `just unlock`, see below), to re-run if the
   access provider renumbers;
7. if **no managed passphrase** unlocks the volume (host installed before the
   switch to shared formatting), asks its installation passphrase one last
   time and enrolls the shared one with it.

Then commit and deploy:

```sh
git add usr/secrets/luks.json
just commit "system(luks): <host>"
just apply <host>
```

At deployment, two idempotent services converge the LUKS header at each
apply: `luks-passphrase-sync` (shared and per-host passphrases) and
`yubikey-luks-enroll` (one FIDO2 entry per YubiKey enrolled on the fleet, see
[Strong authentication](/en/doc/admin-guide/operate/yubikey/)).

:::caution[The initrd key must precede the boot loader]
The initrd secrets are embedded **at boot loader installation**: a key created
after the fact is simply not in the initrd that boots. A brand-new
installation is covered, `just install` drops the key under `/mnt` before
`nixos-install`. On a machine already in service, it is `just luks <host>`
that lays it over SSH: never add a host to `luks.json` by hand, and never
deploy before the key is on the target. When in doubt, `just luks <host> show`
checks it.
:::

{/* t:p 16e9cd0c08aff5bc972154684e2d9f6e1b96d39c78d7f5eb2c0a2499ba80fea1 */}
## Passphrase rotation

- **Per host**: `just luks <host> passwd`, then commit and apply. The old
  keyslot is removed and replaced at the next deployment.
- **Shared**: modify `luks-passphrase` via `just sops`, then apply on the whole
  fleet — each encrypted host rotates its keyslot along the way.

The synchronization service detects changes thanks to a local registry
(`/var/lib/luks-passphrase/ledger`): a modified sops value enrolls the new
passphrase **then** kills the old slot. Slots absent from the registry (FIDO2,
manual enrollments) are never touched.

{/* t:p bbf783e276b624ed050b1c5db6b955e302d82012e3b610b86dace0c2d0c58b4b */}
## Unlocking at boot

Every encrypted host follows the same sequence, with nothing to configure:

1. **YubiKey** — an enrolled key already plugged in blinks its diode: touching it opens the volume, nothing to type. No key plugged in, the step takes ~6 s; a key nobody touches, ~30 s (firmware limit).
2. **Passphrase** — a prompt is then shown, **with no time limit**. It accepts the per-host passphrase, the shared one or the installation one.

Step 2's prompt is **a single request**, served in parallel on the physical console (`/dev/tty1`) and over ssh on port 2222. Nothing to choose, no window to miss: whoever answers first opens the volume.

:::note[How the key is tried]
`fido2-device=` is not written into the `crypttab` entry but into a **separate initrd unit**, `yubikey-luks-unlock-<volume>`, ordered before `systemd-cryptsetup@<volume>` and only tied to it by a `Wants=`: it is allowed to fail, and its failure changes nothing about boot.

This separation avoids two conflicting flaws:

- **the option in the `crypttab` kills boot** when a key is plugged in without anybody touching it: `systemd-cryptsetup` exits with an error instead of offering the passphrase, no prompt is shown — neither console, nor port 2222 — and the initrd falls back to `emergency`. A power outage on an unattended host was enough to immobilize it;
- **without the option, the token is only tried if it is already enumerated**. Yet `hidraw0` appears roughly one second after `systemd-cryptsetup` starts: never in time. The key then silently stops unlocking anything, with nothing in the journal.

What the option really brings is the **wait** for the device (`token-timeout=`, set to 5 s).
:::

Remotely, the initrd starts a **dedicated sshd on port 2222**, with its own persistent host key, reachable only with the `nix` deployment key. Two ways to answer&nbsp;:

```sh
just unlock <hôte>     # sans humain : la passphrase est lue dans sops
just enter <hôte>      # interactif : le prompt est présenté au terminal
```

`just unlock` waits for port 2222 to appear (a dot per attempt), reads the **shared** passphrase from sops and pushes it into the initrd, then checks that the machine has actually left its initrd before announcing success. If it is refused, it retries with the per-host passphrase. That is what `just full-install` calls on its own after the first reboot. On an unencrypted host, it is a logged no-op.

:::note[Why the shared one first]
On the very first boot, the per-host slot does not exist yet: disko formats with the shared passphrase, and `luks-passphrase-sync` only adds the per-host slot from the running system, therefore *after* unlocking. The shared passphrase is the only way in at that point.

Moreover, `dnf-unlock` only knows that it **answered** the request, never whether the passphrase was accepted: systemd just asks again. The only reliable witness is therefore the machine leaving its initrd and answering on port 22, and that is what `just unlock` observes from the administration station.
:::

`just enter` does the same thing interactively&nbsp;: when port 22 does not answer, it probes 2222 on the explicit IP (if given), the host name, then the **WAN IP recorded in the manifest**, and presents the LUKS prompt through `systemd-tty-ask-password-agent`. In both cases, once the passphrase is accepted, boot continues and port 22 comes back.

:::note[Why two paths]
`systemd-tty-ask-password-agent` only talks to a terminal&nbsp;: no script can drive it. The initrd therefore ships a small `dnf-unlock` utility that takes the passphrase from a pipe and answers every pending request. That is the one `just unlock` calls.
:::

:::note[Why the WAN IP?]
A zone's VPN dies with its gateway: when the latter waits for its passphrase, only its WAN leg is reachable. Its initrd moreover replicates production addressing (static IP on the `lan0` bridge, DHCP on the WAN side), since it is itself the DHCP server that its initrd would otherwise wait for. The other hosts simply do DHCP on their wired interfaces.
:::

:::note[Laptops on Wi-Fi]
No networking in initrd without a cable: an encrypted laptop is unlocked at the console (passphrase or YubiKey); remote unlocking does not apply.
:::

:::caution[A NIC driver is not always enough]
A virtio card does not plug into its PCI slot but onto the `virtio0` device created by its **transport** (`virtio_pci`, `virtio_mmio`). Without this module in the initrd, `virtio_net` is never loaded: no link, no IP, and the port-2222 sshd listens into the void — the host waits indefinitely for its passphrase, with nothing in the journal to say so.

Both transports are now bundled by default, and `just luks <hôte> show` compares the drivers actually used by the machine's wired cards with those the built initrd carries.
:::

{/* t:p 1539a3a233d9dca4cb824aa2c0957ab946727829d4ea2da6aa74458c51b27413 */}
## Checking a host

`just luks <hôte> show` writes nothing&nbsp;: it queries the machine and reports. It exits with an error as soon as a check fails, so it can also be used in a script.

```sh
just luks <hôte> show
```

A first block draws up the **keyslot table** of each volume. Nothing is assumed&nbsp;: every managed passphrase is actually tried against the header, so the "Check" column says what the volume accepts today, not what a registry has remembered.

```
/dev/disk/by-partlabel/disk-main-luks
  Slot  Content              Source                       Check
  ----  -------------------  ---------------------------  -----------------
  0     Shared passphrase    sops luks-passphrase         opens the volume
  1     Host passphrase      sops luks/poste/passphrase   opens the volume
  2     YubiKey alice/main   systemd-fido2 token          enrolled
  7     Unknown              -                            not managed by DNF
  Slots in use: 4/32
```

:::note[An "Unknown" slot]
No managed passphrase opens it and no FIDO2 token claims it. Most often it is an interrupted YubiKey enrollment between the keyslot's creation and its token being written — typically during an installation, where passphrase convergence restarts the enrollment service. The service now repairs this case on its own at the next `just apply`: it adopts the keyslot if it belongs to a declared key, deletes it if it is a duplicate. A slot that persists after an apply therefore comes from elsewhere&nbsp;: a manual `systemd-cryptenroll`, for example — DNF never touches it, since it cannot prove who owns it.
:::

A second block audits the **remote unlocking**, point by point&nbsp;:

| Check | What it catches |
|---|---|
| Host present in `luks.json` | The module would stay inert |
| Initrd key on the target (+ fingerprint) | Next apply without remote unlocking |
| sops backup with the same fingerprint | A reinstall would lose remote access |
| initrd sshd enabled in the configuration | `nix.pub` missing, or host outside the manifest |
| Boot loader more recent than the key | **The** trap&nbsp;: key missing from the initrd that boots |
| Wired-card drivers present in the initrd | No stage-1 networking&nbsp;: nothing answers on 2222 |
| Booted system = audited configuration | A pending apply would make the audit misleading |
| WAN IP recorded (gateways) | Unreachable in initrd once the VPN is down |

{/* t:p 018691bf21721672315d60b94b99d685b47b750bf369738c69b4e480c9e4aa59 */}
## Under the hood

| Location | Content | Status |
|---|---|---|
| `usr/secrets/luks.json` | Manifest of provisioned hosts + WAN IP of gateways | Public, versioned |
| `secrets.yaml` → `luks-passphrase` | Shared fleet passphrase | Encrypted sops |
| `secrets.yaml` → `luks/<host>/passphrase` | Per-host passphrase | Encrypted sops |
| `secrets.yaml` → `luks/<host>/initrd-key` | Initrd SSH host key (base64) | Encrypted sops |
| `/var/lib/luks-initrd/` (target) | Working copy of the initrd key | Persistent, outside the repo |
| `/var/lib/luks-passphrase/ledger` (target) | Registry of managed keyslots (rotation detection) | Persistent, outside the repo |

The module exposes two options: `darkone.system.luks.enable` (default `true`)
and `darkone.system.luks.sshPort` (default `2222`).

:::danger[Keyslot budget]
A LUKS2 header contains at most **32 keyslots**. Each enrolled YubiKey consumes
one on **every** encrypted host (fleet policy), on top of the shared and
per-host passphrases. The module warns when the projected total approaches the
limit.
:::

{/* t:p dfceb630353cb65ed8c0887148b11be424125fce0a38013882e9f28165469e56 */}
## See also

- [Strong authentication (YubiKey)](/en/doc/admin-guide/operate/yubikey/): enrollment of physical keys, including the LUKS FIDO2 part
- [Secrets management](/en/doc/admin-guide/operate/secrets/): sops, age keys and rotation
- [The Justfile](/en/doc/admin-guide/operate/justfile/): reference for the `luks`, `enter` and `configure` recipes
