---
title: Security rule catalog
sidebar:
  label: Security rules
  order: 10
lang: en
head:
  - tag: style
    content: |
      /* « Rule catalog » tables (6 col., centered columns) :
         consistent widths across tables. */
      .sl-markdown-content table:has(td[align="center"]) {
        display: table; /* Starlight forces tables to `block`, overriding width:100% */
        table-layout: fixed;
        width: 100%;
      }
      .sl-markdown-content table:has(td[align="center"]) :is(th, td):first-child {
        width: 6.5rem; /* Code : accommodates `R15`–`R27` */
      }
      .sl-markdown-content table:has(td[align="center"]) :is(th, td):nth-child(n+3) {
        width: 3.5rem; /* Min / Inter / Reinforced / High */
      }
---

{/* t:translated-from fr */}
{/* t:h 3f65f89818cab30174dca07696f8c2a9520efc8ee7ac589bde7707c8eeaffc21 */}

{/* t:p 054fae31781777f31e1bd20a2bbc4d3626591128fd0ee30f93ed0f9213b91458 */}
This page summarizes **which ANSSI rules are activated**, according to the **level** and
**category** of a host. It is the reference to consult before hardening a machine.

:::tip[Understand first]
The concepts used here (levels, categories, labels, MAC, egress…) are
explained in [Security
notions](/en/doc/admin-guide/understand/security-notions/).
:::

{/* t:p 68a4c9db7877ba6e627b323b0aa6db0c52e20c58f59a2d03d52b9919e0d2d590 */}
## Reminder: level and category

```nix
darkone.system.security = {
  enable   = true;
  level    = "intermediary";   # minimal | intermediary | reinforced | high
  category = "server";         # base | client | server
};
```

Levels are **cumulative**: `intermediary` adds its rules to those of
`minimal`, and so on. The category adds the rules specific to a workstation
(`client`) or a server (`server`).

{/* t:p 59d6590db674be9de1f7fac85f5ce3c28eac555ac91ff1eddf3e6ad3f14a02b5 */}
## Recommended category per profile

| Profile | Recommended category | Why |
|---|---|---|
| `minimal`, `portable` | `base` | Universal base |
| `desktop`, `laptop` | `client` | Screen, USB, interactive session |
| `server`, `gateway`, `hcs`, `vm` | `server` | Exposed services, network |

:::caution[Today: `base` everywhere]
Automatic profile → category mapping is not yet active. All hosts are
set to `minimal` / `base` by default. To apply a category, set
`category` manually on the relevant host.
:::

{/* t:p e4ce9900a3c7288db0de5450b196339aae6adbe90a222dba1e3771b714eb0867 */}
## Comparison table by level

Each column shows the rule's **state at that level** (levels being
cumulative). Legend:

| Icon | Meaning |
|---|---|
| ✅ | Active at this level |
| ⚠️ | Partial (see note) |
| ❌ | Not yet active at this level |

:::note[Categories]
Rules marked **(server)** or **(client)** only activate with the
corresponding category (`server` or `client`), in addition to the indicated level.
:::

{/* t:p c36904e0276f372fa3beda70f5cec86636e1605193ce2e35fcdd6d40f34ae9ef */}
### Boot and hardware

| Code | Rule | Min | Inter | Rein | High |
|---|---|:-:|:-:|:-:|:-:|
| `R3` | UEFI Secure Boot (if supported) | ❌ | ⚠️ | ⚠️ | ⚠️ |
| `R5` | Bootloader password | ❌ | ⚠️ | ⚠️ | ⚠️ |
| `C6` | LUKS2 disk encryption | ❌ | ⚠️ | ⚠️ | ⚠️ |
| `R7` | IOMMU enabled | ❌ | ❌ | ✅ | ✅ |
| `R29` | `/boot` restricted | ❌ | ❌ | ✅ | ✅ |
| `R6` | cmdline and initramfs protection | ❌ | ❌ | ❌ | ✅ |

:::note[`R3`, `R5`, `C6`: warning, no boot reconfiguration]
From `intermediary` onward, these rules **emit a warning** when the protection
is not detected, but do not reconfigure the boot. Secure Boot (lanzaboote, key
enrollment) and LUKS encryption are **declared in the host disk configuration**
(`disko`); the module only signals their absence. The future `checkScript` will
verify their actual state.
:::

{/* t:p 39ffe2c427bb1dd64be21221d1dd6e7855673b3992e33480da38510659fba48b */}
### Kernel

| Code | Rule | Min | Inter | Renf | High |
|---|---|:-:|:-:|:-:|:-:|
| `R8` | Memory options on the command line | ❌ | ✅ | ✅ | ✅ |
| `R9` | Hardening sysctls | ❌ | ✅ | ✅ | ✅ |
| `R11` | Yama: `ptrace` restricted | ❌ | ✅ | ✅ | ✅ |
| `R10` | Module loading disabled | ❌ | ❌ | ✅ | ✅ |
| `R60` | `linux_hardened` kernel | ❌ | ❌ | ✅ | ✅ |
| `C1` | linux-hardened patches | ❌ | ❌ | ❌ | ✅ |
| `C2` | LSM Lockdown | ❌ | ❌ | ❌ | ✅ |
| `R15`–`R27` | Compile-time hardening | ❌ | ❌ | ❌ | ✅ |

{/* t:p e12981676425489f682f8bdd5c291f7b454a0d43b012ffe810eaca861842839b */}
### Accounts and authentication

| Code | Rule | Min | Inter | Rein | High |
|---|---|:-:|:-:|:-:|:-:|
| `R30` | Disabled unused accounts | ✅ | ✅ | ✅ | ✅ |
| `R31` | Strong passwords | ✅ | ✅ | ✅ | ✅ |
| `R68` | Password storage in `yescrypt` | ✅ | ✅ | ✅ | ✅ |
| `R32` | Lock on inactivity | ❌ | ✅ | ✅ | ✅ |
| `R33` | Admin action traceability | ❌ | ✅ | ✅ | ✅ |
| `R34` | Service accounts without shell | ❌ | ✅ | ✅ | ✅ |
| `R35` | Unique service accounts | ❌ | ✅ | ✅ | ✅ |
| `R67` | Secure remote PAM | ❌ | ⚠️ | ⚠️ | ⚠️ |
| `R69` | User databases over TLS | ❌ | ⚠️ | ⚠️ | ⚠️ |
| `R70` | System accounts ≠ directory | ❌ | ⚠️ | ⚠️ | ⚠️ |
| `C10` | Anti brute-force, session limits | ❌ | ✅ | ✅ | ✅ |

:::note[`R67`, `R69`, `R70`: remote directory deferred]
These rules only concern a **remote directory** (SSSD/nslcd), not used by DNF
today; their TLS aspect is deferred (future `checkScript`). Only **local**
anti-brute-force (`pam_faillock`, see `C10`) is active from `intermediary`.
:::

{/* t:p 528d129ae4723581358ce42fc281b8d52b9487c10c24982d45778aa18b648516 */}
### Administration rights (sudo)

| Code | Rule | Min | Inter | Renf | High |
|---|---|:-:|:-:|:-:|:-:|
| `R39` | Hardened `sudo` directives | ❌ | ✅ | ✅ | ✅ |
| `R40` | Non-root `sudo` targets | ❌ | ✅ | ✅ | ✅ |
| `R42` | Negation prohibition | ❌ | ✅ | ✅ | ✅ |
| `R44` | `sudoedit` for editing | ❌ | ✅ | ✅ | ✅ |
| `R38` | Dedicated `sudo` group | ❌ | ❌ | ✅ | ✅ |
| `R41` | Limited `NOEXEC` overrides | ❌ | ❌ | ✅ | ✅ |

{/* t:p ce99852047f4ca6a5a71c15d5fa508052467924d30f4591db6f648155ce8a897 */}
### Files and permissions

| Code | Rule | Min | Inter | Rein | High |
|---|---|:-:|:-:|:-:|:-:|
| `R53` | Orphaned file detection | ✅ | ✅ | ✅ | ✅ |
| `R54` | Sticky bit on `/tmp` and `/var/tmp` | ✅ | ✅ | ✅ | ✅ |
| `R56` | `setuid` detection outside whitelist | ✅ | ✅ | ✅ | ✅ |
| `R14` | Filesystem sysctls | ❌ | ✅ | ✅ | ✅ |
| `R28` | Partitioning and mount options | ❌ | ✅ | ✅ | ✅ |
| `R50` | Sensitive file restriction | ❌ | ✅ | ✅ | ✅ |
| `R52` | Socket and named pipe permissions | ❌ | ⚠️ | ⚠️ | ⚠️ |
| `R55` | Per-user temporary directories | ❌ | ⚠️ | ⚠️ | ⚠️ |
| `R36` | `UMASK` 0077 | ❌ | ❌ | ✅ | ✅ |
| `R57` | Minimal root `setuid` (capabilities) | ❌ | ❌ | ✅ | ✅ |

:::note[`R28`, `R52`, `R55`: designated services and partitions]
`R28` hardens `/tmp`, `/proc` and `/dev/shm` by default; options on `/var/log`,
`/srv`, `/opt`, `/home` are added via `darkone.security.filesystem.extraMountHardening`
(separate partitions required). `R52` (`0750` mode for runtime directories)
and `R55` (private `/tmp` per service) apply to units listed in
`darkone.security.services.hardenedUnits` — empty by default. See `R63`.
:::

{/* t:p 1dfc54618127e4183dc0a6137f2382e47156604b5c525a114b9baaca9ef7097b */}
### Services and confinement

| Code | Rule | Min | Inter | Rein | High |
|---|---|:-:|:-:|:-:|:-:|
| `R62` | Obsolete services disabled | ✅ | ✅ | ✅ | ✅ |
| `R63` | Reduced service capabilities | ❌ | ⚠️ | ⚠️ | ⚠️ |
| `R37` | Mandatory access control (MAC) | ❌ | ❌ | ✅ | ✅ |
| `R45` | AppArmor profiles | ❌ | ❌ | ✅ | ✅ |
| `R64` | Restricted service privileges | ❌ | ❌ | ✅ | ✅ |
| `R65` | Service isolation (namespaces) | ❌ | ❌ | ✅ | ✅ |
| `R66` | Container hardening | ❌ | ❌ | ❌ | ✅ |

:::note[`R63`: opt-in sandboxing per service]
Full systemd confinement (`ProtectSystem=strict`, system call filters,
`MemoryDenyWriteExecute`, `UMask=0027`…) applies to units declared in
`darkone.security.services.hardenedUnits`. **Empty by default**: blind
hardening would break services. The list acts as a *floor*, overridable per
unit, and `needs-jit` lifts `MemoryDenyWriteExecute` for JIT engines.
:::

{/* t:p f463c066b8dde638f3eab2eb0c7daef12a51a6115c6ca12c1e651cd5a1a0745d */}
### Network

| Code | Rule | Min | Inter | Renf | High |
|---|---|:-:|:-:|:-:|:-:|
| `R80` | Monitoring listening ports | ✅ | ✅ | ✅ | ✅ |
| `C4` | Firewall default deny | ⚠️ | ⚠️ | ✅ | ✅ |
| `R12` | IPv4 network sysctls | ❌ | ✅ | ✅ | ✅ |
| `R13` | IPv6 disabled (unless `no-ipv6`) | ❌ | ✅ | ✅ | ✅ |
| `C5` | OpenSSH hardening | ❌ | ✅ | ✅ | ✅ |
| `C7` | NTS time synchronization | ❌ | ✅ | ✅ | ✅ |
| `C8` | Secure DNS resolver (DNSSEC, DoT) | ❌ | ✅ | ✅ | ✅ |
| `R79` | Hardened exposed services **(server)** | ❌ | ✅ | ✅ | ✅ |
| `R78` | Network service isolation **(server)** | ❌ | ❌ | ✅ | ✅ |

:::note[`C4` partial at the first two levels]
At `minimal` and `intermediary` levels, `C4` covers **inbound** filtering
(default deny). **Outbound** filtering (egress) is added from
`reinforced` and above.
:::

{/* t:p f59cb27147ba7402e4a96f7569e41a10ad12531fce1d1012e41269834820e6aa */}
### Logs and integrity

| Code | Rule | Min | Inter | Renf | High |
|---|---|:-:|:-:|:-:|:-:|
| `R71` | Persisting sealed logging | ❌ | ❌ | ✅ | ✅ |
| `R72` | Dedicated logs per service | ❌ | ❌ | ✅ | ✅ |
| `R73` | auditd with ANSSI rules | ❌ | ❌ | ✅ | ✅ |
| `C9` | Core dumps disabled | ❌ | ❌ | ✅ | ✅ |
| `R76` | Sealing and integrity verification | ❌ | ❌ | ❌ | ✅ |
| `R77` | Sealed base protection | ❌ | ❌ | ❌ | ✅ |

{/* t:p dfd3fa0a642fcb615177eda75e932b7da2c4238ef2a88927d66782954d051cfa */}
### System and miscellaneous

| Code | Rule | Min | Inter | Hard | High |
|---|---|:-:|:-:|:-:|:-:|
| `C11` | Legal access banners | ✅ | ✅ | ✅ | ✅ |
| `C12` | `cron` / `at` restricted | ✅ | ✅ | ✅ | ✅ |
| `R61` | Regular updates | ⚠️ | ⚠️ | ⚠️ | ⚠️ |
| `R74` | Hardened local mail | ❌ | ✅ | ✅ | ✅ |
| `R75` | Mail alias (`root` → admin) | ❌ | ✅ | ✅ | ✅ |
| `R51` | Installation secret rotation | ❌ | ❌ | ✅ | ✅ |
| `C3` | USBGuard **(workstation)** | ❌ | ❌ | ✅ | ✅ |

:::note[`R61` : managed by DNF]
Updates are **centrally** managed by the declarative deployment. The
`autoUpgrade` mechanism is therefore disabled at all levels.
:::

:::caution[Tag `kernel-recompile`]
The `high`-level kernel hardening (R15 to R27, C1) requires **recompiling** the
kernel. On a machine without this need, add `kernel-recompile` to `excludes` to
ignore it.
:::

{/* t:p bd52fe6aa55ab1a5b3abfa55798db39b658200d3d37d3a18346916037adb1aab */}
## Adapt without breaking everything

Two safety valves allow bypassing a specific protection without lowering the
global level.

- **Tags** (`excludes`): exclude a **group** of incompatible rules
  (e.g. `needs-jit`, `no-ipv6`).
- **Exceptions** (`exceptions`): bypass **one** rule, justification
  required.

Details on these safety valves are in [Security
concepts](/en/doc/admin-guide/understand/security-notions/#exclusion-labels).

:::danger[Module under stabilization]
Raise the level **progressively** and test each host before production.
:::

{/* t:p e7e75fc2664cff04022043f12392c2bbcb3e4ba07577b01d877932b81320a7ca */}
## See also

- [Security concepts](/en/doc/admin-guide/understand/security-notions/): concepts for reading this catalog
- [Security and hardening](/en/doc/admin-guide/maintain/security/): day-to-day implementation
- [Security modules reference](/en/ref/modules/security/): all options exposed by the code
