---
title: Project architecture
sidebar:
  label: Architecture
  order: 1
lang: en
---

{/* t:translated-from fr */}
{/* t:h 4227f5cd535a8aebcc4ec701b59926b80fefbf63b8ae0b6da4da5a501bdf4a3f */}

{/* t:p b5c93ed401aeac0423940d48fb14415d49df4c21135a3687cb38d5016c941e86 */}
The framework is organized in layers: each level serves the one above, from the
base system up to host and user profiles.

{/* t:p 20e51e64d084c20b63249123be24faed1333cccf7cd7874cd9d5f4c78a843ddf */}
## File organization

The complete structure and abstraction layers are described in the
[project overview](/en/doc/#file-layout).

- `dnf/` : the framework : modules, Home Manager configuration, libraries.
- `usr/` : the local project (writable) : configuration, secrets, machines.
- `var/generated/` : generated files (do not edit by hand).
- `src/generator/` : generator sources (Rust).
- `doc/` : this documentation.

{/* t:p 612c2b3b727e2162a06c225b1ba018ba78f00a8918971764572149fa164f91cd */}
## Reference modules

The ready-to-use modules are documented in the
[module reference](/en/ref/modules/), sorted by category (system, service,
security, console, graphic, admin, user, mixin, home).

{/* t:p fe3953c97b85bafb668392df027bf8185b14ad2479e774852b57a1059e2a0160 */}
## Abstraction layers

Each level builds on the previous one: the base system serves the modules, which compose the profiles, which define the hosts and users.

- **[Complete file structure](/en/doc/#structure)** : detailed tree.
- **[Layers diagram](/en/doc/#abstraction-layers)** : from the system to the profiles.

| Layer | Where | Role |
|---|---|---|
| **Modules** | `dnf/modules/standard/` | Unit building blocks (a service, a system setting). |
| **Mixins** | `dnf/modules/mixin/` | Macro-modules: host profiles, profile complements. |
| **Home** | `dnf/home/` | [Home Manager](https://github.com/nix-community/home-manager) modules and profiles (users). |
| **Library** | `dnf/lib/` | Shared helpers (`dnfLib`): network, OIDC, firewall, security... |
| **Packages** | `dnf/pkgs/` | Programs [absent from nixpkgs](/en/doc/dev-guide/packages/), exposed as `pkgs.<name>`. |
| **Generation** | `src/generator/` | Transform `etc/config.yaml` → `var/generated/`. |

{/* t:p 2afbebe6aedb4e4c5b04e5100d2e2a8b7b3753414a47bae256d11f60205cdfdf */}
## Framework vs local project

The framework (`dnf/`) is **reusable and rarely modified**; the project (`usr/`)
**overrides** it without touching it.

- `usr/modules/` mirrors `dnf/modules/`: a file at the same path **overrides** or **completes** the framework module.
- `usr/home/` mirrors `dnf/home/` for user profiles and modules.
- `usr/machines/<hôte>/` carries the host, sorted by origin ([detail](/en/doc/how-to/admin/machines-layout/)): `configuration.nix` (specific settings, the only file to edit), `install/` (disk and `stateVersion`, frozen at installation), `hardware/` (recorded on the machine).
- `usr/secrets/` contains encrypted secrets ([sops](/en/doc/admin-guide/operate/secrets/)).

:::tip[Golden rule]
We modify `dnf/` to evolve **the framework** (useful to everyone), `usr/` to
adapt **a specific network**. Never edit `var/generated/`.
:::

{/* t:p c314d006e46e028eb49f62ccd76d0a7b101136458329a816a554828f52e7fb57 */}
## Arguments injected into modules

DNF modules receive, in addition to standard NixOS arguments, specific arguments
via `specialArgs` (the generator and `flake.nix` provide them):

| Argument | Content |
|---|---|
| `config`, `lib`, `pkgs` | Standard NixOS arguments. |
| `dnfLib` | Shared DNF helpers (see [Modules](/en/doc/dev-guide/modules/)). |
| `dnfConfig` | Global configuration (network ports, default values). |
| `host` | The current host: `hostname`, `users`, `services`, `features`, `profile`. |
| `hosts` | The list of **all** hosts on the network. |
| `network` | Network topology: zones, domain, services. |
| `zone` | The current host's zone. |
| `workDir` | Project root (absolute paths). |

:::note[Accuracy]
The exact list of arguments for a module is visible at the top of its file (the
`{ lib, config, dnfLib, host, ... }` block). When in doubt, check the source.
:::

{/* t:p b3fb71681be75c1a0868f3f43eb52b487dc4be9333ac85f7c21e42744204fa70 */}
## Generated overlays

Some values don't belong in `etc/config.yaml` (manual): too technical, or **provisioned by a tool**. The pattern: a script writes a `var/generated/<x>.nix` file that the assembler `dnf/lib/mk-configuration.nix` **merges** into an injected argument.

Example: `just configure-alert-bot` writes `var/generated/matrix.nix` (alert bot identity + room IDs), merged into `network.matrix.*` via `recursiveUpdate` (the generated file wins). Modules read `network.matrix` without knowing where it comes from.

:::tip[When to use]
For **derived or provisioned** data, never edited by hand, outside of `config.yaml`. The overlay is **optional**: if absent, the argument keeps its base value.
:::
