---
title: Contributing to the documentation
sidebar:
  label: Documentation
  order: 4
lang: en
---

{/* t:translated-from fr */}
{/* t:h b65af93c81bdfbc46ea403bd5c7959dc9e57935864cd15a0778cd7d92fee0c0b */}

{/* t:p 74a257b30cb1d4b3c2a8d9b3499ead893ba02e9202469abcd92c73473ea98567 */}
This site is built with [Astro](https://astro.build/) and
[Starlight](https://starlight.astro.build/). Pages live under
`src/content/docs/<lang>/` (`fr`, `en`).

{/* t:p 6b83178770c6b771dd3f8d6dca700e1b49f544533bc67292d6df88fb12140963 */}
## Where to write

- `doc/<section>.mdx` : **entry page** of a section, introduction + links.
- `doc/<section>/<feuille>.mdx` : **detail pages**.
- Short cross-cutting topics → `doc/how-to/{user,admin,dev}/`.

Write in **French** (`fr/`) ; English is produced afterwards by
`just translate`. Slugs and file names stay in English.

{/* t:p 2bc5be94c96c567dc98778689d5237fe6b378aa15fc77fdfe15c869b54a8701e */}
## Essential Rules

- Max 7 headings per level, depth 3.
- `<Steps>` for step sequences, `<Aside>` (with title) for callouts.
- `Card`/`CardGrid` are reserved for the user guide.
- See the [help & how-to dev](/en/doc/how-to/dev/) and role *skills*.

{/* t:p 2a85f99d0f3e9e1a5c3d8b717f1371cf4216366032c0a11bcee7997eaad7076a */}
## Tools

```sh
just dev      # development server
just tags     # refresh translation tags
just build    # build the static site
just test     # tests + internal link validation
```

:::caution[Do not touch]
The `{/* t:* */}` markers and generated pages (`*/ref/modules.mdx`) are managed
by scripts. See `AGENTS.md`.
:::

{/* t:p 4e3e70428dca276ff16231c600da69320ade5d62fc3d5b1a0275eeb17ba6d1d7 */}
## Languages and translation

Write **only in French** (`fr/`) ; English (`en/`) is **produced**
by `just translate`. Never edit `en/` manually.

| Action | To do |
|---|---|
| **Create** a page | In `fr/` only ; do not run `just translate`. |
| **Move** a page | Move **both** languages together (files, verbatim), then `just tags`. |
| **Translate** | At the end of the work: `just tags` then `just translate`. |

Translation markers are managed by the scripts, **never manually**:

| Marker | Role |
|---|---|
| `{/* t:main */}` | Source-of-truth file for a page. |
| `{/* t:translated-from <lang> */}` | Translated file (do not modify). |
| `{/* t:h <hash> */}` | Frontmatter hash. |
| `{/* t:p <hash> */}` | Paragraph hash (before each block). |

:::caution[Translated file = read-only]
A file bearing `t:translated-from` is **never** modified: it is
overwritten by `just translate`. Edit the `fr/` source (`t:main`).
:::

{/* t:p 091c2721f294a4aed8fdf11b6b3bb7aa3b0789b29af5a1ba3ca31433cbe306b1 */}
## Links and anchors

- Internal links in `/fr/...`; validated at build by `starlight-links-validator`.
- An **anchor** = *github-slugger* slug of the target heading text
  (`## Mon Titre` → `#mon-titre`).
- Check: `just build` (fails on a dead link) or `just check-links`.

{/* t:p 10421bb3b19dc1b30577c0ffbacc0dbffc4e8322281950668400eba061ab61d1 */}
## Making beautiful pages

The validated recipe (short items, colored callouts, `<Steps>`, tables,
D2 diagrams, generic examples) is detailed in `AGENTS.md` → *Making "beautiful" pages*. To be followed according to the persona&nbsp;:

| Persona | Tone |
|---|---|
| **User** | Inspire, no jargon; `Card`/`LinkCard` allowed. |
| **Administrator** | Precise, operational; tables and callouts. |
| **Developer / AI** | Hierarchy (where → detail), step by step, concise. |

:::danger[Public data]
This documentation is **public**: never include data from the private network (`../`).
Use generic examples (`domain.tld`, documentation IPs).
:::
